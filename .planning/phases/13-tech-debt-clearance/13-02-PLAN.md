---
phase: 13-tech-debt-clearance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - pyproject.toml
autonomous: true
requirements:
  - DEBT-03
  - DEBT-04
  - DEBT-05
  - DEBT-06
  - DEBT-07

must_haves:
  truths:
    - "docs/README.md migration description says migrations require explicit `novel db migrate` — no auto-apply claim"
    - "docs/README.md export subcommands are `chapter` and `all` — no `regenerate` mention"
    - "docs/README.md error contract table shows `requires_action: str` for GateViolation — not `requires_action: true`"
    - "docs/README.md gate tables are `architecture_gate` and `gate_checklist_items` — not `gate_certifications` / `gate_checklist_log`"
    - "pyproject.toml lists pydantic>=2.11 as a direct dependency"
  artifacts:
    - path: "docs/README.md"
      provides: "Accurate system architecture overview"
      contains: "novel db migrate"
    - path: "pyproject.toml"
      provides: "Declared pydantic direct dependency"
      contains: "pydantic>=2.11"
  key_links:
    - from: "docs/README.md"
      to: "Gate System section"
      via: "table name correction"
      pattern: "architecture_gate.*gate_checklist_items"
    - from: "pyproject.toml"
      to: "dependencies list"
      via: "direct dependency declaration"
      pattern: "pydantic>=2.11"
---

<objective>
Fix four factual bugs in docs/README.md and add pydantic as a declared direct dependency in pyproject.toml.

Purpose: The README contains four incorrect statements that will mislead system administrators: a false auto-apply claim for migrations, a non-existent `regenerate` export subcommand, a wrong field type in the error contract table, and two wrong table names in the gate section. Separately, pydantic is only a transitive dependency — declaring it directly makes the v2 requirement explicit and prevents silent breakage if the mcp package's transitive dependency changes.
Output: Corrected docs/README.md (4 bug fixes); pyproject.toml with pydantic>=2.11 in dependencies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Confirmed bug locations extracted from source before planning. -->

DEBT-03 — False auto-apply claim (docs/README.md line ~41):
  Current: "All migrations run at startup via `novel db migrate` or are
            applied automatically on `uv run novel-mcp`."
  Fix: Remove "or are applied automatically on `uv run novel-mcp`" — the MCP server
       does NOT auto-apply migrations. Requires a pre-initialized database.
  Accurate replacement: "All migrations must be applied explicitly via `novel db migrate`
                          before starting the MCP server."

DEBT-04 — Wrong export subcommand (docs/README.md line ~73):
  Current: `novel export` — chapter markdown export: `regenerate`
  Actual subcommands (confirmed in src/novel/export/cli.py):
    - `chapter` (line 116: @app.command())
    - `all` (line 149: @app.command(name="all"))
  NOTE: The CONTEXT.md decision text said "chapter and export-all" but that is STALE.
  The real code registers the second subcommand as `name="all"`, not `name="export-all"`.
  This plan uses the confirmed source truth: subcommands are `chapter` and `all`.
  Fix: Change the subcommand list line to: `novel export` — chapter markdown export: `chapter`, `all`

DEBT-05 — Wrong GateViolation field type (docs/README.md line 137):
  Current error contract table row:
    | Gate violation | Gated tool called before gate certification | `requires_action: true` with a `message` field |
  Actual GateViolation model (src/novel/models/shared.py line 17-19):
    class GateViolation(BaseModel):
        requires_action: str   ← str, not bool; holds the message directly (no separate field)
  Fix: Change to: `requires_action: str` (the string IS the message — no separate `message` field)
  New table row: | Gate violation | Gated tool called before gate certification | `requires_action: str` — the string describes the required action |

DEBT-06 — Wrong gate table names (docs/README.md line ~122):
  Current: "the gate state is stored in `gate_certifications` and `gate_checklist_log`"
  Actual tables (from src/novel/gate/cli.py SQL): `architecture_gate` and `gate_checklist_items`
  Fix: Replace both wrong names with correct names.

DEBT-07 — Missing pydantic direct dependency (pyproject.toml):
  Current dependencies section:
    dependencies = [
        "typer>=0.24.0",
        "aiosqlite>=0.17.0",
        "mcp>=1.26.0,<2.0.0",
    ]
  Fix: Add `"pydantic>=2.11"` to the list (matching existing >=major.minor style).
  pyproject.toml uses `>=major.minor` style for aiosqlite and typer, and `>=major.minor.patch`
  for mcp. Use `>=2.11` (no patch) to match the aiosqlite/typer pattern and avoid
  over-constraining future mcp upgrades.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix four factual bugs in docs/README.md</name>
  <files>docs/README.md</files>
  <action>
Apply four targeted corrections to docs/README.md. Read the file first to get exact current text, then make these changes:

**Fix 1 — DEBT-03: Remove false auto-apply migration claim (around line 41).**
Find the sentence:
  "All migrations run at startup via `novel db migrate` or are
  applied automatically on `uv run novel-mcp`."
Replace with:
  "All migrations must be applied explicitly via `novel db migrate` before starting
  the MCP server; the server requires a pre-initialized database."

**Fix 2 — DEBT-04: Correct export subcommand name (around line 73).**
Find the line in the CLI subcommand group list:
  `- `novel export` — chapter markdown export: `regenerate``
Replace with:
  `- `novel export` — chapter markdown export: `chapter`, `all``

Note: The CONTEXT.md decision text said "export-all" but that was stale. Source-confirmed
subcommand names from src/novel/export/cli.py are `chapter` (line 116) and `all` (line 149,
registered as `name="all"`). Use `all`, not `export-all`.

**Fix 3 — DEBT-05: Correct GateViolation field type in error contract table (line 137).**
Find the table row:
  `| Gate violation | Gated tool called before gate certification | `requires_action: true` with a `message` field |`
Replace with:
  `| Gate violation | Gated tool called before gate certification | `requires_action: str` — the string describes the required action |`

Rationale: GateViolation has one field `requires_action: str`. It is not a bool. There is no separate `message` field — the string IS the message.

**Fix 4 — DEBT-06: Correct gate table names (around line 122).**
Find the sentence:
  "the gate state is stored in `gate_certifications` and `gate_checklist_log`."
Replace with:
  "the gate state is stored in `architecture_gate` and `gate_checklist_items`."

Do not change any other content. The rest of docs/README.md is accurate.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
text = open('/Users/gary/writing/drafter/docs/README.md').read()
failures = []
if 'applied automatically on' in text:
    failures.append('FAIL DEBT-03: auto-apply claim still present')
if 'regenerate' in text:
    failures.append('FAIL DEBT-04: regenerate subcommand still present')
if 'requires_action: true' in text:
    failures.append('FAIL DEBT-05: wrong requires_action type still present')
if 'gate_certifications' in text or 'gate_checklist_log' in text:
    failures.append('FAIL DEBT-06: wrong table names still present')
if failures:
    print('\n'.join(failures)); sys.exit(1)
else:
    print('PASS: all four README bugs corrected')
"
    </automated>
  </verify>
  <done>
- No occurrence of "applied automatically on `uv run novel-mcp`" in docs/README.md
- No occurrence of "regenerate" in the CLI subcommand section of docs/README.md
- Error contract table shows `requires_action: str` (not `requires_action: true`)
- Gate section references `architecture_gate` and `gate_checklist_items` (not the wrong names)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pydantic as a declared direct dependency</name>
  <files>pyproject.toml</files>
  <action>
Add `"pydantic>=2.11"` to the `dependencies` list in pyproject.toml.

Current dependencies block:
```toml
dependencies = [
    "typer>=0.24.0",
    "aiosqlite>=0.17.0",
    "mcp>=1.26.0,<2.0.0",
]
```

Target dependencies block (add pydantic after aiosqlite, alphabetically before mcp):
```toml
dependencies = [
    "typer>=0.24.0",
    "aiosqlite>=0.17.0",
    "mcp>=1.26.0,<2.0.0",
    "pydantic>=2.11",
]
```

The `>=2.11` constraint matches the version currently pulled transitively through mcp>=1.26.0
and makes the Pydantic v2 requirement explicitly visible. Use `>=major.minor` style (no patch),
consistent with typer and aiosqlite entries. Do not run `uv add` — edit pyproject.toml
directly to preserve the existing formatting and comment style.

After editing, verify with: `uv pip show pydantic` or inspect the file directly.
  </action>
  <verify>
    <automated>python3 -c "
import sys
text = open('/Users/gary/writing/drafter/pyproject.toml').read()
if 'pydantic>=2.11' in text:
    print('PASS: pydantic>=2.11 declared in pyproject.toml')
else:
    print('FAIL: pydantic not found in dependencies'); sys.exit(1)
"
    </automated>
  </verify>
  <done>pyproject.toml dependencies list includes `"pydantic>=2.11"`. Inspecting pyproject.toml confirms pydantic is a declared direct dependency alongside typer, aiosqlite, and mcp.</done>
</task>

</tasks>

<verification>
Run all checks together:

```bash
python3 -c "
import sys
readme = open('docs/README.md').read()
toml = open('pyproject.toml').read()
failures = []
if 'applied automatically on' in readme:
    failures.append('FAIL DEBT-03: auto-apply claim still present')
if 'regenerate' in readme:
    failures.append('FAIL DEBT-04: regenerate still in README')
if 'requires_action: true' in readme:
    failures.append('FAIL DEBT-05: wrong requires_action type')
if 'gate_certifications' in readme or 'gate_checklist_log' in readme:
    failures.append('FAIL DEBT-06: wrong gate table names')
if 'pydantic>=2.11' not in toml:
    failures.append('FAIL DEBT-07: pydantic not in pyproject.toml')
if failures:
    print('\n'.join(failures)); sys.exit(1)
else:
    print('PASS: all 5 requirements satisfied')
"
```
</verification>

<success_criteria>
- docs/README.md: migration description is accurate (no auto-apply claim)
- docs/README.md: export subcommands listed as `chapter` and `all` (no `regenerate`)
- docs/README.md: GateViolation error contract row shows `requires_action: str`
- docs/README.md: gate section names `architecture_gate` and `gate_checklist_items`
- pyproject.toml: `"pydantic>=2.11"` in the dependencies list
</success_criteria>

<output>
After completion, create `.planning/phases/13-tech-debt-clearance/13-02-SUMMARY.md`
</output>
