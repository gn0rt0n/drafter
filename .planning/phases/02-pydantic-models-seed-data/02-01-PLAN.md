---
phase: 02-pydantic-models-seed-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/novel/models/shared.py
  - src/novel/models/world.py
  - src/novel/models/characters.py
  - src/novel/models/relationships.py
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Shared error contract types (NotFoundResponse, ValidationFailure, GateViolation) exist and are importable"
    - "World domain models (Book, Era, Act, Culture, Faction, Location, Artifact, ObjectState, FactionPoliticalState) exist with all SQL columns represented"
    - "Character domain models (Character, CharacterKnowledge, CharacterBelief, CharacterLocation, InjuryState, TitleState) exist with all SQL columns represented"
    - "Relationship domain models (CharacterRelationship, RelationshipChangeEvent, PerceptionProfile) exist with all SQL columns represented"
    - "JSON TEXT fields have @field_validator(mode='before') + to_db_dict() on every model that needs it"
    - "All nullable columns use X | None = None union syntax (not Optional[X])"
  artifacts:
    - path: "src/novel/models/shared.py"
      provides: "NotFoundResponse, ValidationFailure, GateViolation"
      exports: ["NotFoundResponse", "ValidationFailure", "GateViolation"]
    - path: "src/novel/models/world.py"
      provides: "Book, Era, Act, Culture, Faction, Location, Artifact, ObjectState, FactionPoliticalState"
      exports: ["Book", "Era", "Act", "Culture", "Faction", "Location", "Artifact", "ObjectState", "FactionPoliticalState"]
    - path: "src/novel/models/characters.py"
      provides: "Character, CharacterKnowledge, CharacterBelief, CharacterLocation, InjuryState, TitleState"
      exports: ["Character", "CharacterKnowledge", "CharacterBelief", "CharacterLocation", "InjuryState", "TitleState"]
    - path: "src/novel/models/relationships.py"
      provides: "CharacterRelationship, RelationshipChangeEvent, PerceptionProfile"
      exports: ["CharacterRelationship", "RelationshipChangeEvent", "PerceptionProfile"]
  key_links:
    - from: "src/novel/models/characters.py"
      to: "src/novel/migrations/007_characters.sql"
      via: "field names must match PRAGMA table_info(characters) column names exactly"
    - from: "src/novel/models/world.py"
      to: "src/novel/migrations/002_core_books_eras.sql + 003_acts.sql + 004_cultures.sql + 005_factions.sql + 006_locations.sql + 010_artifacts.sql"
      via: "field names match SQL column names exactly — verified by TEST-01 in plan 02-04"
---

<objective>
Create the shared error contract types and the four core entity domain model files.
These are the foundation models that Phase 3+ MCP tools import directly.

Purpose: Provide typed Pydantic v2 models for all entity domains so Phase 3 can do `from novel.models import Character` immediately. The shared error types establish the MCP error contract interface (returned by tools in Phase 3+).

Output: shared.py (3 error types), world.py (9 models), characters.py (6 models), relationships.py (3 models)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pydantic-models-seed-data/02-RESEARCH.md

## Migration source files (schema ground truth)
@src/novel/migrations/002_core_books_eras.sql
@src/novel/migrations/003_acts.sql
@src/novel/migrations/004_cultures.sql
@src/novel/migrations/005_factions.sql
@src/novel/migrations/006_locations.sql
@src/novel/migrations/007_characters.sql
@src/novel/migrations/010_artifacts.sql
@src/novel/migrations/012_relationships.sql
@src/novel/migrations/013_character_state.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared.py with error contract types</name>
  <files>src/novel/models/shared.py</files>
  <action>
    Create `src/novel/models/shared.py` with exactly three Pydantic v2 BaseModel classes:

    ```python
    from pydantic import BaseModel

    class NotFoundResponse(BaseModel):
        """Returned by MCP tools when a record is not found. Never raise — return this."""
        not_found_message: str

    class ValidationFailure(BaseModel):
        """Returned by MCP tools on validation failure. Never raise — return this."""
        is_valid: bool = False
        errors: list[str]

    class GateViolation(BaseModel):
        """Returned by prose-phase tools when architecture gate is not certified."""
        requires_action: str
    ```

    These are not DB-table models — they have no `id` field and no `to_db_dict()`. They are pure response envelopes for the MCP error contract that Phase 3 tools will return.

    No `__all__` needed in this file — `__init__.py` handles re-exports.
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "from novel.models.shared import NotFoundResponse, ValidationFailure, GateViolation; print('OK')"</automated>
  </verify>
  <done>All three classes importable. NotFoundResponse has `not_found_message: str`. ValidationFailure has `is_valid: bool = False` and `errors: list[str]`. GateViolation has `requires_action: str`.</done>
</task>

<task type="auto">
  <name>Task 2: Create world.py, characters.py, relationships.py</name>
  <files>src/novel/models/world.py, src/novel/models/characters.py, src/novel/models/relationships.py</files>
  <action>
    Read each migration file (listed in context) and derive the model fields. Every field name must match the SQL column name exactly — TEST-01 in plan 02-04 compares `model.model_fields` against `PRAGMA table_info` column names.

    **world.py** — covers migrations 002, 003, 004, 005, 006, 010:
    - `Book`: id, title, series_order, status, word_count_target, canon_status, created_at, updated_at (from 002)
    - `Era`: id, name, sequence_order, description, start_marker, end_marker, certainty_level, canon_status, created_at, updated_at (from 002)
    - `Act`: id, book_id, act_number, title, summary, start_chapter_id, end_chapter_id, canon_status, created_at, updated_at (from 003)
    - `Culture`: id, name, region, language_family, naming_conventions, social_structure, taboos, values, aesthetics, notes, canon_status, created_at, updated_at (from 004)
    - `Faction`: id, name, faction_type, ideology, goals, territory, leader_character_id, founding_era_id, status, notes, canon_status, created_at, updated_at (from 005)
    - `Location`: id, name, location_type, parent_location_id, culture_id, controlling_faction_id, description, sensory_profile, strategic_value, accessibility, notable_features, notes, canon_status, source_file, created_at, updated_at (from 006) — `sensory_profile` is JSON TEXT (`dict | None`)
    - `Artifact`: id, name, artifact_type, description, current_owner_id, current_location_id, home_era_id, significance, notes, canon_status, created_at, updated_at (from 010)
    - `ObjectState`: id, artifact_id, chapter_id, condition, owner_character_id, location_id, notes, created_at (from 010)
    - `FactionPoliticalState`: (covered in plan 02-03 via migration 021)

    Wait — check 021 for faction_political_states. If in 021, move to world.py here but note 021 is in plan 02-03. Actually, include FactionPoliticalState in world.py even though the table is defined in 021. Read 021 for the columns: id, faction_id, chapter_id, power_level, territory_size, active_conflicts, internal_stability, notable_events, notes, created_at.

    **characters.py** — covers migration 007, 013:
    - `Character`: id, name, role, faction_id, culture_id, home_era_id, age, physical_description, personality_core, backstory_summary, secret, motivation, fear, flaw, strength, arc_summary, voice_signature, notes, canon_status, source_file, created_at, updated_at, reviewed_at (from 007)
    - `CharacterKnowledge`: id, character_id, chapter_id, knowledge_type, content, source, is_secret, notes, created_at (from 013) — `is_secret: bool = False`
    - `CharacterBelief`: id, character_id, belief_text, belief_type, confidence_level, formed_in_chapter_id, challenged_in_chapter_id, resolved_in_chapter_id, notes, created_at (from 013)
    - `CharacterLocation`: id, character_id, chapter_id, location_id, arrival_context, departure_context, notes, created_at (from 013)
    - `InjuryState`: id, character_id, chapter_id, injury_description, severity, is_active, recovery_notes, notes, created_at (from 013) — `is_active: bool = True`
    - `TitleState`: id, character_id, chapter_id, title_text, granted_by, is_active, notes, created_at (from 013) — `is_active: bool = True`

    **relationships.py** — covers migration 012:
    - `CharacterRelationship`: id, character_a_id, character_b_id, relationship_type, dynamic, trust_level, power_dynamic, shared_history, conflict_source, notes, canon_status, established_in_chapter_id, created_at, updated_at (from 012)
    - `RelationshipChangeEvent`: id, relationship_id, chapter_id, event_description, trust_delta, change_type, notes, created_at (from 012)
    - `PerceptionProfile`: id, perceiver_id, target_id, chapter_id, perceived_role, perceived_trustworthiness, perceived_threat_level, key_assumptions, blind_spots, emotional_valence, notes, created_at, updated_at (from 012)

    **Patterns to apply (from RESEARCH.md):**
    - All ids: `id: int | None = None`
    - Nullable FKs and nullable columns: `field: TYPE | None = None`
    - Boolean INTEGER 0/1 columns: `field: bool = False` (Pydantic v2 coerces 0/1 automatically)
    - `sensory_profile` TEXT JSON in Location: add `@field_validator("sensory_profile", mode="before") @classmethod` with json.loads if isinstance str, plus `to_db_dict()` method
    - Use `X | None` syntax, NOT `Optional[X]`
    - Use `from pydantic import BaseModel, field_validator` (only import field_validator in files that need it)
    - Use `import json` only in files with JSON fields

    Verify each file is importable before moving to the next:
    `uv run python -c "from novel.models.world import Book, Location"`
    `uv run python -c "from novel.models.characters import Character, CharacterKnowledge"`
    `uv run python -c "from novel.models.relationships import CharacterRelationship"`
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "from novel.models.world import Book, Era, Act, Culture, Faction, Location, Artifact, ObjectState, FactionPoliticalState; from novel.models.characters import Character, CharacterKnowledge, CharacterBelief, CharacterLocation, InjuryState, TitleState; from novel.models.relationships import CharacterRelationship, RelationshipChangeEvent, PerceptionProfile; print('OK')"</automated>
  </verify>
  <done>All 18 model classes importable. Location.sensory_profile accepts both `None`, a dict, and a JSON string. CharacterKnowledge.is_secret coerces 0/1 to bool. All field names match their migration SQL column names exactly (verified by inspecting model_fields vs migration files).</done>
</task>

</tasks>

<verification>
All four model files importable. Location JSON field round-trips correctly:
`uv run python -c "from novel.models.world import Location; l = Location(name='Test', sensory_profile='{\"sight\": \"dark\"}'); print(type(l.sensory_profile)); print(l.to_db_dict()['sensory_profile'][:1])"` — should print `<class 'dict'>` then `{`.
</verification>

<success_criteria>
- shared.py: 3 error types importable with correct fields
- world.py: 9 model classes importable, Location has @field_validator + to_db_dict()
- characters.py: 6 model classes importable, boolean fields coerce correctly
- relationships.py: 3 model classes importable
- All field names match SQL column names from migration files
- No `Optional[X]` syntax (Python 3.12, use `X | None`)
- No `model.dict()` or `Model.parse_obj()` anywhere (Pydantic v2: use model_dump() / model_validate())
</success_criteria>

<output>
After completion, create `.planning/phases/02-pydantic-models-seed-data/02-01-SUMMARY.md`
</output>
