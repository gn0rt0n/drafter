---
phase: 07-session-timeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/novel/models/timeline.py
  - src/novel/tools/timeline.py
  - src/novel/mcp/server.py
  - tests/test_session.py
  - tests/test_timeline.py
autonomous: true
requirements:
  - TIME-06
  - TIME-07
  - TIME-08

must_haves:
  truths:
    - "Claude can validate whether travel between locations is realistic given elapsed in-story time"
    - "Claude can create or update a timeline event"
    - "Claude can create or update a POV chronological position"
    - "All 18 session and timeline tools are wired into server.py and callable via MCP"
    - "In-memory MCP client tests cover all 18 tools and pass"
  artifacts:
    - path: "src/novel/models/timeline.py"
      provides: "TravelValidationResult model added alongside existing timeline models"
      contains: "TravelValidationResult"
    - path: "src/novel/tools/timeline.py"
      provides: "8 timeline tools total (5 reads from 07-02 + 3 writes/validation added here)"
      contains: "validate_travel_realism, upsert_event, upsert_pov_position"
    - path: "src/novel/mcp/server.py"
      provides: "session and timeline tools registered"
      contains: "session.register(mcp), timeline.register(mcp)"
    - path: "tests/test_session.py"
      provides: "In-memory MCP tests for all 10 session tools"
    - path: "tests/test_timeline.py"
      provides: "In-memory MCP tests for all 8 timeline tools"
  key_links:
    - from: "src/novel/mcp/server.py"
      to: "src/novel/tools/session.py"
      via: "session.register(mcp)"
      pattern: "session.register"
    - from: "src/novel/mcp/server.py"
      to: "src/novel/tools/timeline.py"
      via: "timeline.register(mcp)"
      pattern: "timeline.register"
    - from: "validate_travel_realism"
      to: "TravelValidationResult"
      via: "returns TravelValidationResult with is_realistic, issues, segment"
      pattern: "TravelValidationResult"
---

<objective>
Add `TravelValidationResult` model, complete timeline.py with 3 write/validation tools, wire both modules into server.py, and write comprehensive in-memory MCP tests for all 18 session and timeline tools.

Purpose: Delivers the full Phase 7 capability — all 18 tools are registered in the server and tested via the in-memory FastMCP client protocol.

Output: TravelValidationResult in models/timeline.py, timeline.py with 8 tools total, server.py wired for Phase 7, test_session.py and test_timeline.py with full MCP protocol coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-session-timeline/07-CONTEXT.md
@.planning/phases/07-session-timeline/07-RESEARCH.md
@.planning/phases/07-session-timeline/07-01-SUMMARY.md
@.planning/phases/07-session-timeline/07-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

TravelValidationResult to define in src/novel/models/timeline.py:
```python
class TravelValidationResult(BaseModel):
    is_realistic: bool
    issues: list[str]
    segment: TravelSegment | None = None
```

validate_travel_realism validation logic (locked decisions from CONTEXT.md):
- Takes: travel_segment_id: int | None = None, character_id: int | None = None
- Fetches segment(s): if travel_segment_id, fetch one from travel_segments WHERE id=?; if character_id, fetch all WHERE character_id=?
- For each segment, apply checks:
  1. elapsed_days must be non-null and > 0 (hard failure: is_realistic=False)
  2. travel_method must be non-null (hard failure: is_realistic=False)
  3. Advisory: if travel_method == 'walking' and elapsed_days < 1 → flag "Suspicious: walking travel with less than 1 day elapsed"
  4. Advisory: if from_location_id or to_location_id is None → flag "Incomplete: missing location endpoint"
- If travel_segment_id provided and no segment found: return TravelValidationResult(is_realistic=False, issues=["Travel segment not found"], segment=None)
- If character_id provided and no segments: return TravelValidationResult(is_realistic=True, issues=[], segment=None)
- For single segment validation: return TravelValidationResult with segment included
- For character (multiple segments): return TravelValidationResult aggregating all issues across segments (is_realistic=all passing), segment=None

upsert_event pattern (locked decision from CONTEXT.md):
- None-id branch: INSERT INTO events (...) VALUES (...) → cursor.lastrowid → SELECT back
- provided-id branch: INSERT INTO events (id, ...) VALUES (?, ...) ON CONFLICT(id) DO UPDATE SET ... → SELECT back

upsert_pov_position pattern (locked decision from CONTEXT.md):
- ON CONFLICT(character_id, chapter_id) DO UPDATE (natural unique key)

Test patterns (from prior phases):
- MCP session entered PER-TEST inside _call_tool coroutine (not fixture) — anyio cancel scope incompatible
- FastMCP serializes list[T] as N TextContent blocks → `[json.loads(c.text) for c in result.content]`
- Single object: `json.loads(result.content[0].text)`
- Session-scoped DB fixture with `tmp_path_factory` and `minimal` seed
- `monkeypatch.setenv("NOVEL_DB_PATH", test_db_path)` inside _call_tool

From src/novel/mcp/server.py (current state):
```python
# Phase 6 registration is last line before run():
gate.register(mcp)
# Add after this:
# Register domain tools — Phase 7
session.register(mcp)
timeline.register(mcp)
```

_call_tool pattern from test_gate.py:
```python
async def _call_tool(db_path: str, tool_name: str, args: dict):
    os.environ["NOVEL_DB_PATH"] = db_path
    async with create_connected_server_and_client_session(mcp) as session:
        result = await session.call_tool(tool_name, args)
    return result
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TravelValidationResult model + 3 timeline tools + server.py wiring</name>
  <files>src/novel/models/timeline.py, src/novel/tools/timeline.py, src/novel/mcp/server.py</files>
  <action>
**Step 1: Add TravelValidationResult to src/novel/models/timeline.py**

Read the existing file. After the last existing class (PovChronologicalPosition), append:
```python
class TravelValidationResult(BaseModel):
    """Result of travel realism validation for a travel segment or character."""
    is_realistic: bool
    issues: list[str]
    segment: TravelSegment | None = None
```
No other changes to the file.

**Step 2: Extend src/novel/tools/timeline.py with 3 more tools**

Read the existing file. Add to the imports at top:
`from novel.models.timeline import Event, TravelSegment, PovChronologicalPosition, TravelValidationResult`

Inside the existing `register()` function, append 3 more tool functions after the 5 read tools:

**validate_travel_realism(travel_segment_id: int | None = None, character_id: int | None = None) -> TravelValidationResult | GateViolation**
(Locked validation logic from CONTEXT.md)
1. check_gate first.
2. Validation: if both None → return TravelValidationResult(is_realistic=False, issues=["Must provide travel_segment_id or character_id"], segment=None).
3. Fetch segments:
   - If travel_segment_id: `SELECT * FROM travel_segments WHERE id = ?`. If empty → return TravelValidationResult(is_realistic=False, issues=["Travel segment not found"], segment=None).
   - If character_id: `SELECT * FROM travel_segments WHERE character_id = ? ORDER BY id`. If empty → return TravelValidationResult(is_realistic=True, issues=[], segment=None).
4. For each segment, run validation:
   ```
   issues = []
   if seg["elapsed_days"] is None or seg["elapsed_days"] <= 0:
       issues.append("elapsed_days must be non-null and greater than 0")
   if seg["travel_method"] is None:
       issues.append("travel_method must be non-null")
   if seg["travel_method"] == "walking" and seg["elapsed_days"] is not None and seg["elapsed_days"] < 1:
       issues.append("Suspicious: walking travel with less than 1 day elapsed")
   if seg["from_location_id"] is None or seg["to_location_id"] is None:
       issues.append("Incomplete: missing location endpoint")
   ```
5. Return:
   - Single segment (travel_segment_id): `TravelValidationResult(is_realistic=len(issues)==0, issues=issues, segment=TravelSegment(**dict(rows[0])))`
   - Multiple segments (character_id): aggregate all issues from all segments; `TravelValidationResult(is_realistic=len(all_issues)==0, issues=all_issues, segment=None)`

**upsert_event(name: str, event_type: str | None = None, chapter_id: int | None = None, location_id: int | None = None, in_story_date: str | None = None, duration: str | None = None, summary: str | None = None, significance: str | None = None, notes: str | None = None, canon_status: str | None = None, event_id: int | None = None) -> Event | GateViolation**
(Locked two-branch pattern from CONTEXT.md)
- None-id branch (event_id is None):
  `INSERT INTO events (name, event_type, chapter_id, location_id, in_story_date, duration, summary, significance, notes, canon_status) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`. Get lastrowid. Commit.
- Provided-id branch:
  `INSERT INTO events (id, name, event_type, chapter_id, location_id, in_story_date, duration, summary, significance, notes, canon_status) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET name=excluded.name, event_type=excluded.event_type, chapter_id=excluded.chapter_id, location_id=excluded.location_id, in_story_date=excluded.in_story_date, duration=excluded.duration, summary=excluded.summary, significance=excluded.significance, notes=excluded.notes, canon_status=excluded.canon_status`. Commit.
- SELECT back: `SELECT * FROM events WHERE id = ?`. Return `Event(**dict(rows[0]))`.

**upsert_pov_position(character_id: int, chapter_id: int, in_story_date: str | None = None, day_number: int | None = None, location_id: int | None = None, notes: str | None = None) -> PovChronologicalPosition | GateViolation**
(Locked UNIQUE key pattern from CONTEXT.md)
`INSERT INTO pov_chronological_position (character_id, chapter_id, in_story_date, day_number, location_id, notes) VALUES (?, ?, ?, ?, ?, ?) ON CONFLICT(character_id, chapter_id) DO UPDATE SET in_story_date=excluded.in_story_date, day_number=excluded.day_number, location_id=excluded.location_id, notes=excluded.notes`. Commit.
SELECT back: `SELECT * FROM pov_chronological_position WHERE character_id = ? AND chapter_id = ?`. Return `PovChronologicalPosition(**dict(rows[0]))`.

**Step 3: Wire session and timeline into src/novel/mcp/server.py**

Read the existing server.py. Add to the import line:
From: `from novel.tools import characters, relationships, chapters, scenes, world, magic, plot, arcs, gate`
To: `from novel.tools import characters, relationships, chapters, scenes, world, magic, plot, arcs, gate, session, timeline`

After `gate.register(mcp)`, add:
```python
# Register domain tools — Phase 7
session.register(mcp)
timeline.register(mcp)
```
Update the module docstring Phase comment to include "session and timeline tools registered."
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "
from novel.mcp.server import mcp
tools = {t.name for t in mcp._tool_manager.list_tools()}
phase7 = {'start_session','close_session','get_last_session','log_agent_run','get_project_metrics','log_project_snapshot','get_pov_balance','get_open_questions','log_open_question','answer_open_question','get_pov_positions','get_pov_position','get_event','list_events','get_travel_segments','validate_travel_realism','upsert_event','upsert_pov_position'}
missing = phase7 - tools
assert not missing, f'Missing tools: {missing}'
print(f'All 18 Phase 7 tools registered in server. Total tools: {len(tools)}')
"</automated>
  </verify>
  <done>
- TravelValidationResult class exists in src/novel/models/timeline.py
- timeline.py has 8 tools total (5 reads + validate_travel_realism + upsert_event + upsert_pov_position)
- server.py imports and registers session and timeline modules
- `uv run python -c "from novel.mcp.server import mcp"` completes without error
- All 18 Phase 7 tools visible in mcp._tool_manager.list_tools()
  </done>
</task>

<task type="auto">
  <name>Task 2: Write in-memory MCP tests for all 18 session and timeline tools</name>
  <files>tests/test_session.py, tests/test_timeline.py</files>
  <action>
Create two test files following the exact pattern from `tests/test_gate.py`. Both use the `minimal` seed, session-scoped DB fixture, and per-test MCP session entry via `_call_tool`.

**tests/test_session.py**

Module docstring explaining: 10 session tools tested, gate_ready seed required because all tools are prose-phase (call check_gate which requires gate certified), per-test MCP session, FastMCP serialization rules.

Use `gate_ready` seed (NOT minimal) — all 10 tools are prose-phase and call check_gate.

Session-scoped fixture:
```python
@pytest.fixture(scope="session")
def test_db_path(tmp_path_factory):
    db_file = tmp_path_factory.mktemp("session_db") / "test_session.db"
    conn = sqlite3.connect(str(db_file))
    conn.execute("PRAGMA foreign_keys=ON")
    conn.execute("PRAGMA journal_mode=WAL")
    apply_migrations(conn)
    load_seed_profile(conn, "gate_ready")
    conn.commit()
    conn.close()
    return str(db_file)
```

_call_tool helper (same pattern as test_gate.py):
```python
async def _call_tool(db_path: str, tool_name: str, args: dict):
    os.environ["NOVEL_DB_PATH"] = db_path
    async with create_connected_server_and_client_session(mcp) as session:
        result = await session.call_tool(tool_name, args)
    return result
```

Write at minimum these tests:

1. `test_start_session` — call start_session({}), parse result.content[0].text as JSON, assert "id" in result dict and result["closed_at"] is None (new session is open).

2. `test_close_session` — first call start_session to get a new session id, then call close_session with that session_id and summary="test summary", assert closed result has "closed_at" not None and "summary" == "test summary". Also assert "carried_forward" is a list (JSON-decoded).

3. `test_get_last_session` — call get_last_session({}), parse result, assert "id" in result (some session exists after gate_ready seed).

4. `test_log_agent_run` — call log_agent_run({agent_name: "TestAgent", tool_name: "get_character", success: True}), parse result, assert result["agent_name"] == "TestAgent" and result["id"] > 0.

5. `test_get_project_metrics` — call get_project_metrics({}), parse result, assert "word_count" in result and "chapter_count" in result and result["chapter_count"] > 0 (gate_ready seed has chapters).

6. `test_log_project_snapshot` — call log_project_snapshot({}), parse result, assert result["id"] > 0 and "word_count" in result and "snapshot_at" in result.

7. `test_get_pov_balance` — call get_pov_balance({}), parse list (may be empty if seed has no pov_character_id on chapters, or has entries), assert result is a list.

8. `test_log_open_question` — call log_open_question({question: "Will the hero survive?", domain: "plot"}), parse result, assert result["question"] == "Will the hero survive?" and result["answered_at"] is None.

9. `test_get_open_questions` — call get_open_questions({}), parse list, assert it is a list (may have the question from test 8 — use separate DB isolation via ordering, not assuming specific count).

10. `test_answer_open_question` — call log_open_question to create a question, get its id, then call answer_open_question({question_id: id, answer: "Yes"}), parse result, assert result["answer"] == "Yes" and result["answered_at"] is not None.

**tests/test_timeline.py**

Module docstring explaining: 8 timeline tools, gate_ready seed, per-test MCP session.

Use `gate_ready` seed.

Session-scoped fixture with `"timeline_db"` dir name.

_call_tool identical pattern.

Write at minimum these tests:

1. `test_upsert_event_create` — call upsert_event({name: "The Battle of Dawn", event_type: "conflict", chapter_id: 1}), parse result, assert result["name"] == "The Battle of Dawn" and result["id"] > 0.

2. `test_upsert_event_update` — create event first, then call upsert_event with the returned id and updated name="The Battle of Dusk", assert result["name"] == "The Battle of Dusk".

3. `test_get_event` — after upsert, call get_event({event_id: N}), assert result["id"] == N.

4. `test_get_event_not_found` — call get_event({event_id: 99999}), parse result, assert "not_found_message" in result.

5. `test_list_events` — call list_events({}), parse list, assert it is a list (may be empty or have items).

6. `test_list_events_by_chapter` — upsert an event with chapter_id=1, call list_events({chapter_id: 1}), assert list is non-empty and all items have chapter_id == 1.

7. `test_upsert_pov_position_create` — call upsert_pov_position({character_id: 1, chapter_id: 1, day_number: 1}), parse result, assert result["character_id"] == 1 and result["chapter_id"] == 1.

8. `test_upsert_pov_position_update` — call upsert_pov_position again with same character_id=1, chapter_id=1, day_number=5, assert result["day_number"] == 5 (ON CONFLICT DO UPDATE).

9. `test_get_pov_positions` — call get_pov_positions({chapter_id: 1}), parse list, assert it is a list and any item has chapter_id == 1.

10. `test_get_pov_position` — call get_pov_position({character_id: 1, chapter_id: 1}), parse result, assert result["character_id"] == 1.

11. `test_get_pov_position_not_found` — call get_pov_position({character_id: 99999, chapter_id: 99999}), assert "not_found_message" in result.

12. `test_get_travel_segments` — call get_travel_segments({character_id: 1}), parse list, assert it is a list (may be empty).

13. `test_validate_travel_realism_no_args` — call validate_travel_realism({}), parse result, assert result["is_realistic"] == False and len(result["issues"]) > 0.

14. `test_validate_travel_realism_segment_not_found` — call validate_travel_realism({travel_segment_id: 99999}), assert result["is_realistic"] == False and "not found" in result["issues"][0].

Important serialization notes:
- `validate_travel_realism`, `upsert_event`, `upsert_pov_position`, `get_event`, `get_pov_position` return single objects: `json.loads(result.content[0].text)`.
- `list_events`, `get_pov_positions`, `get_travel_segments`, `get_open_questions`, `get_pov_balance` return lists: `[json.loads(c.text) for c in result.content]`. Empty list if result.content is empty.
- `log_open_question`, `log_agent_run`, `log_project_snapshot` return single objects.

All test functions must be `@pytest.mark.anyio` decorated.
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run pytest tests/test_session.py tests/test_timeline.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
- tests/test_session.py exists with at least 10 test functions
- tests/test_timeline.py exists with at least 14 test functions
- `uv run pytest tests/test_session.py tests/test_timeline.py` passes with 0 failures
- All 18 tools exercise the full MCP protocol path
  </done>
</task>

</tasks>

<verification>
```
cd /Users/gary/writing/drafter && uv run pytest tests/test_session.py tests/test_timeline.py -v 2>&1 | tail -40
```
All tests pass. Then verify full suite has not regressed:
```
cd /Users/gary/writing/drafter && uv run pytest --tb=short 2>&1 | tail -20
```
</verification>

<success_criteria>
- TravelValidationResult defined in src/novel/models/timeline.py
- timeline.py registers 8 tools: 5 reads (from 07-02) + validate_travel_realism + upsert_event + upsert_pov_position
- server.py registers all 18 Phase 7 tools (session + timeline modules)
- test_session.py and test_timeline.py both exist and pass with 0 failures
- Full test suite passes (no regressions in prior phase tests)
- Total MCP tool count in server = prior count (49 tools from phases 3-6) + 18 = 67 tools
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-timeline/07-03-SUMMARY.md`
</output>
