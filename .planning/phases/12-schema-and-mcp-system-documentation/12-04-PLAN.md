---
phase: 12-schema-and-mcp-system-documentation
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schema.md
  - docs/README.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "docs/schema.md documents all 71 tables (magic_use_log and practitioner_abilities entries present)"
    - "docs/README.md states 103 tools across 18 modules in all four locations where counts appear"
  artifacts:
    - path: "docs/schema.md"
      provides: "Complete SQLite schema reference including magic_use_log and practitioner_abilities"
      contains: "### `magic_use_log`"
    - path: "docs/schema.md"
      provides: "practitioner_abilities table entry with UNIQUE constraint"
      contains: "### `practitioner_abilities`"
    - path: "docs/README.md"
      provides: "Accurate tool and module counts throughout"
      contains: "103 tools"
  key_links:
    - from: "docs/schema.md World section Mermaid diagram"
      to: "magic_use_log and practitioner_abilities table entries"
      via: "table name references in erDiagram block"
      pattern: "magic_use_log|practitioner_abilities"
    - from: "docs/mcp-tools.md Magic domain Tables touched"
      to: "docs/schema.md"
      via: "table names log_magic_use, get_practitioner_abilities, check_magic_compliance reference"
      pattern: "magic_use_log|practitioner_abilities"
---

<objective>
Close the two gaps identified in 12-VERIFICATION.md: add missing magic_use_log and practitioner_abilities table entries to docs/schema.md, and correct wrong tool and module counts in docs/README.md.

Purpose: docs/schema.md claims to document all 71 tables but only has 69. docs/README.md states "121 tools across 19 modules" in four places when the actual counts are 103 tools across 18 modules. Readers following mcp-tools.md Magic domain tool cards will find table names with no schema documentation.

Output: docs/schema.md with 71 table entries; docs/README.md with accurate counts in all four locations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-schema-and-mcp-system-documentation/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add magic_use_log and practitioner_abilities entries to docs/schema.md</name>
  <files>docs/schema.md</files>
  <action>
Read `src/novel/migrations/021_literary_publishing.sql` lines 212–235 to confirm exact field names and types before writing. The two tables are defined as:

**magic_use_log** (lines 212–223):
- id INTEGER PRIMARY KEY AUTOINCREMENT
- chapter_id INTEGER NOT NULL REFERENCES chapters(id)
- scene_id INTEGER REFERENCES scenes(id) — nullable
- character_id INTEGER NOT NULL REFERENCES characters(id)
- magic_element_id INTEGER REFERENCES magic_system_elements(id) — nullable
- action_description TEXT NOT NULL
- cost_paid TEXT — nullable
- compliance_status TEXT NOT NULL DEFAULT 'compliant'
- notes TEXT — nullable
- created_at TEXT NOT NULL DEFAULT (datetime('now'))
No UNIQUE constraint — append-only audit log.

**practitioner_abilities** (lines 225–235):
- id INTEGER PRIMARY KEY AUTOINCREMENT
- character_id INTEGER NOT NULL REFERENCES characters(id)
- magic_element_id INTEGER NOT NULL REFERENCES magic_system_elements(id)
- proficiency_level INTEGER NOT NULL DEFAULT 1
- acquired_chapter_id INTEGER REFERENCES chapters(id) — nullable
- notes TEXT — nullable
- created_at TEXT NOT NULL DEFAULT (datetime('now'))
- updated_at TEXT NOT NULL DEFAULT (datetime('now'))
- UNIQUE(character_id, magic_element_id)

**Step 1 — Update the World section Mermaid ER diagram** (schema.md lines 340–465):
Locate the closing backticks of the erDiagram block (after the `object_states }o--|| artifacts` line). Before the closing triple-backtick line, add two new entity blocks and two relationship lines inside the diagram:

```
    magic_use_log {
        INTEGER id PK
        INTEGER chapter_id FK
        INTEGER scene_id FK
        INTEGER character_id FK
        INTEGER magic_element_id FK
        TEXT action_description
        TEXT cost_paid
        TEXT compliance_status
        TEXT notes
        TEXT created_at
    }
    practitioner_abilities {
        INTEGER id PK
        INTEGER character_id FK
        INTEGER magic_element_id FK
        INTEGER proficiency_level
        INTEGER acquired_chapter_id FK
        TEXT notes
        TEXT created_at
        TEXT updated_at
    }
    magic_use_log }o--|| chapters : "chapter_id"
    magic_use_log }o--o| scenes : "scene_id"
    magic_use_log }o--|| characters : "character_id"
    magic_use_log }o--o| magic_system_elements : "magic_element_id"
    practitioner_abilities }o--|| characters : "character_id"
    practitioner_abilities }o--|| magic_system_elements : "magic_element_id"
    practitioner_abilities }o--o| chapters : "acquired_chapter_id"
```

Also update the Cross-domain FKs note line (the blockquote after the Mermaid block, around line 467) to append: `magic_use_log.chapter_id → chapters.id` and `magic_use_log.character_id → characters.id` (Characters). `practitioner_abilities.character_id → characters.id` (Characters). `practitioner_abilities.acquired_chapter_id → chapters.id` (Chapters).

**Step 2 — Add table entries** before the `## 4. Characters` heading (currently around line 667).

Insert two new table entries after the `object_states` entry (after its `---` divider) and before the `## 4. Characters` line:

```markdown
### `magic_use_log`

Append-only audit trail of every magic use event during the story. Records who used magic, which element, what it cost, and whether it was compliant with the magic system rules. Each row is an immutable event — no upsert semantics.

| Field | Type | Description |
|-------|------|-------------|
| `id` | INTEGER PK | Primary key |
| `chapter_id` | INTEGER FK | References `chapters.id` — chapter in which the magic was used |
| `scene_id` | INTEGER FK | References `scenes.id` — scene context (nullable) |
| `character_id` | INTEGER FK | References `characters.id` — character who used magic |
| `magic_element_id` | INTEGER FK | References `magic_system_elements.id` — element invoked (nullable) |
| `action_description` | TEXT | Description of the magic action performed |
| `cost_paid` | TEXT | Cost paid by the character (nullable — may not apply to all elements) |
| `compliance_status` | TEXT | Whether use was compliant with magic rules (default: `compliant`) |
| `notes` | TEXT | Standard annotation field |
| `created_at` | TEXT | Standard audit timestamp |

**Populated by:** `log_magic_use` (world domain). Append-only — each row is a permanent record.

---

### `practitioner_abilities`

Tracks which magic system elements a character has the ability to use, and at what proficiency level. One row per character-element pair; the unique constraint prevents duplicate capability records.

| Field | Type | Description |
|-------|------|-------------|
| `id` | INTEGER PK | Primary key |
| `character_id` | INTEGER FK | References `characters.id` — the practitioner |
| `magic_element_id` | INTEGER FK | References `magic_system_elements.id` — the element the character can use |
| `proficiency_level` | INTEGER | Skill level (default: 1; higher = more proficient) |
| `acquired_chapter_id` | INTEGER FK | References `chapters.id` — chapter when ability was acquired (nullable) |
| `notes` | TEXT | Standard annotation field |
| `created_at` | TEXT | Standard audit timestamp |
| `updated_at` | TEXT | Standard audit timestamp |

**Constraints:** `UNIQUE(character_id, magic_element_id)` — one ability record per character per magic element.

**Populated by:** `get_practitioner_abilities` reads; direct DB insert or seed for writes (no dedicated MCP write tool).

---
```
  </action>
  <verify>
    <automated>grep -c "^### \`" /Users/gary/writing/drafter/docs/schema.md</automated>
  </verify>
  <done>
`grep -c "^### \`" docs/schema.md` returns 71 (was 69). Both `### \`magic_use_log\`` and `### \`practitioner_abilities\`` entries are present in the World section before `## 4. Characters`. The Mermaid erDiagram block for the World section includes both table entity definitions and their relationship lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix tool and module counts in docs/README.md</name>
  <files>docs/README.md</files>
  <action>
Make four targeted string replacements in docs/README.md. Read the file first to confirm exact line contents, then apply:

1. Line 21: `│   121 tools across 19 modules` → `│   103 tools across 18 modules`
2. Line 52: `with 121 tools organized into` → `with 103 tools organized into`
3. Line 53: `19 domain modules under` → `18 domain modules under`
4. Line 93: `all 121 tools are available` → `all 103 tools are available`
5. Line 151: `all 121 tools across 19 domains` → `all 103 tools across 18 domains`

Note: The VERIFICATION.md lists lines 21, 52, 93, 151 for "121 tools" (4 replacements) and lines 21, 53 for "19 modules/domains" (2 replacements). The replacement at line 21 covers both the tool count and module count in the same string. All five replacement targets are correct per the line contents confirmed by grep.

After editing, verify no instances of "121" or "19 module" remain in the file.
  </action>
  <verify>
    <automated>grep -c "121" /Users/gary/writing/drafter/docs/README.md && echo "expected: 0"</automated>
  </verify>
  <done>
`grep "121" docs/README.md` returns no matches. `grep "103 tools" docs/README.md` returns 4 matches. `grep "18 module" docs/README.md` returns 2 matches. No "19 module" strings remain.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# Confirm 71 table entries in schema.md
grep -c "^### \`" /Users/gary/writing/drafter/docs/schema.md
# Expected: 71

# Confirm both new tables present
grep "^### \`magic_use_log\`\|^### \`practitioner_abilities\`" /Users/gary/writing/drafter/docs/schema.md
# Expected: 2 matches

# Confirm no stale counts in README
grep "121\|19 module" /Users/gary/writing/drafter/docs/README.md
# Expected: no output

# Confirm correct counts appear
grep "103 tools" /Users/gary/writing/drafter/docs/README.md | wc -l
# Expected: 4

grep "18 module" /Users/gary/writing/drafter/docs/README.md | wc -l
# Expected: 2
```
</verification>

<success_criteria>
- docs/schema.md has 71 table entries (grep -c "^### \`" returns 71)
- magic_use_log and practitioner_abilities entries are in the World section with accurate field tables derived from migration 021 SQL
- The World section Mermaid erDiagram includes both new tables and their FK relationships
- docs/README.md contains zero instances of "121" or "19 module"
- docs/README.md contains "103 tools" in 4 places and "18 module" in 2 places
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-and-mcp-system-documentation/12-04-SUMMARY.md`
</output>
