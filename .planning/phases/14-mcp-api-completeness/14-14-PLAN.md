---
phase: 14-mcp-api-completeness
plan: "14"
type: execute
wave: 3
depends_on: ["14-13"]
files_modified:
  - src/novel/tools/world.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "Claude Code can create, read, list, and delete artifacts (magic items, relics, key objects)"
    - "Claude Code can log, read, and delete object_states (item state changes during the story)"
    - "delete_artifact refuses with ValidationFailure when object_states or event_artifacts reference it"
    - "log_object_state follows log_* pattern (append-only, artifact_id FK check)"
  artifacts:
    - path: "src/novel/tools/world.py"
      provides: "get_artifact, list_artifacts, upsert_artifact, delete_artifact, get_object_states, log_object_state, delete_object_state tools"
      contains: "upsert_artifact"
  key_links:
    - from: "upsert_artifact"
      to: "artifacts table"
      via: "two-branch upsert"
      pattern: "artifacts"
    - from: "log_object_state"
      to: "object_states table"
      via: "log_* with artifact_id FK check"
      pattern: "object_states"
---

<objective>
Add full CRUD for artifacts and object_states in world.py.

Purpose: artifacts has zero MCP coverage — Claude Code cannot read or write artifact data at all. object_states (tracks item state changes during the story, e.g., sword broken at ch 10) also has zero coverage. Both belong in world.py alongside locations.

Note: Depends on 14-13 (which added books/acts/eras to world.py). Both this plan and 14-13 modify world.py, so they run sequentially: 14-13 → 14-14.

Output: get_artifact, list_artifacts, upsert_artifact, delete_artifact, get_object_states, log_object_state, delete_object_state — 7 new tools in world.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-mcp-api-completeness/14-RESEARCH.md
@.planning/phases/14-mcp-api-completeness/14-13-SUMMARY.md

<interfaces>
<!-- Key types from src/novel/models/world.py. -->

From src/novel/models/world.py:
```python
class Artifact(BaseModel):
    id: int
    name: str
    artifact_type: str  # e.g., "weapon", "relic", "document"
    description: str | None
    current_holder: str | None   # or character_id FK — check migration
    origin_era_id: int | None    # FK to eras
    notes: str | None
    created_at: str | None

class ObjectState(BaseModel):
    id: int
    artifact_id: int
    chapter_id: int
    state_description: str
    notes: str | None
    created_at: str | None
```

FK relationships:
- artifacts ← object_states (artifact_id FK), event_artifacts (artifact_id FK)
- object_states ← nothing (leaf log table)

world.py may not yet import Artifact and ObjectState (they existed in novel.models.world but no tools used them). Add to imports.

Note: Read src/novel/models/world.py lines 130-165 to confirm exact Artifact and ObjectState fields before implementing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add artifacts full CRUD to world.py</name>
  <files>src/novel/tools/world.py</files>
  <behavior>
    - get_artifact(artifact_id): Artifact | NotFoundResponse
    - list_artifacts(): list[Artifact] — all artifacts ordered by name
    - upsert_artifact(artifact_id, name, artifact_type, ...): two-branch upsert. Artifact | ValidationFailure
    - delete_artifact(artifact_id): FK-safe (object_states and event_artifacts reference artifacts). NotFoundResponse | ValidationFailure | dict
  </behavior>
  <action>
    Read src/novel/tools/world.py (post 14-13) and src/novel/models/world.py lines 130-165 to get the exact Artifact model fields and column names.

    Add Artifact and ObjectState to world.py imports from novel.models.world if not already present:
    `from novel.models.world import Artifact, Act, Book, Culture, Era, Faction, FactionPoliticalState, Location, ObjectState`
    (adjust to include all currently imported models plus the new ones)

    Add 4 tools inside register(mcp):

    get_artifact: SELECT by id from artifacts. Return Artifact | NotFoundResponse.

    list_artifacts: SELECT all from artifacts ORDER BY name. Return list[Artifact].

    upsert_artifact: two-branch upsert. Parameters: artifact_id (int|None), name (required), artifact_type (required), and optional fields from Artifact model (description, notes, origin_era_id if applicable). Return: Artifact | ValidationFailure.

    delete_artifact: FK-safe. artifacts is referenced by object_states (artifact_id) and event_artifacts (artifact_id — junction table for events). Use full FK-safe try/except. Return: NotFoundResponse | ValidationFailure | dict.

    No gate check. No print(). Update docstring.
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && python -c "from novel.tools.world import register; print('OK')"</automated>
  </verify>
  <done>get_artifact, list_artifacts, upsert_artifact, delete_artifact importable; delete_artifact uses FK-safe pattern</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add object_states full CRUD to world.py</name>
  <files>src/novel/tools/world.py</files>
  <behavior>
    - get_object_states(artifact_id): list[ObjectState] — all states for an artifact ordered by chapter_id. Returns list (empty if none)
    - log_object_state(artifact_id, chapter_id, state_description, notes): log_* pattern with artifact_id + chapter_id FK checks. ObjectState | NotFoundResponse | ValidationFailure
    - delete_object_state(object_state_id): log-style delete. NotFoundResponse | dict
  </behavior>
  <action>
    Continue editing world.py. Read it again to see state after Task 1.

    Read src/novel/models/world.py to confirm ObjectState model fields and object_states table column names.

    Confirm ObjectState is in the imports (added in Task 1). Add if missed.

    Add 3 tools inside register(mcp):

    get_object_states: requires artifact_id. SELECT from object_states WHERE artifact_id = ? ORDER BY chapter_id. Return list[ObjectState] (always a list, never null).

    log_object_state: log_* pattern. Pre-check artifact_id exists in artifacts. Pre-check chapter_id exists in chapters. INSERT into object_states. Select back by lastrowid. Return ObjectState. Parameters: artifact_id (required), chapter_id (required), state_description (required), notes (optional). Return: ObjectState | NotFoundResponse | ValidationFailure.

    delete_object_state: log-style (object_states is a leaf table). Table: object_states, column: id. Return: NotFoundResponse | dict.

    Update register() docstring. No gate check. No print().
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && python -c "from novel.tools.world import register; print('OK')"</automated>
  </verify>
  <done>All 7 artifact/object_state tools importable; log_object_state follows log_* pattern; Artifact and ObjectState in imports</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /Users/gary/writing/drafter && python -c "from novel.tools.world import register; print('OK')"
```

Confirm tools:
```bash
cd /Users/gary/writing/drafter && grep -n "async def get_artifact\|async def list_artifacts\|async def upsert_artifact\|async def delete_artifact\|async def get_object_states\|async def log_object_state\|async def delete_object_state" src/novel/tools/world.py
```
Expected: 7 matching lines.
</verification>

<success_criteria>
- 7 new tools: get_artifact, list_artifacts, upsert_artifact, delete_artifact, get_object_states, log_object_state, delete_object_state
- upsert_artifact is two-branch
- delete_artifact uses FK-safe pattern (object_states and event_artifacts reference it)
- log_object_state does FK pre-checks for both artifact_id and chapter_id
- Artifact and ObjectState imported in world.py
- world.py imports cleanly
- No print(), no gate checks
</success_criteria>

<output>
After completion, create `.planning/phases/14-mcp-api-completeness/14-14-SUMMARY.md`
</output>
