---
phase: 15-documentation-restructure
plan: 04
type: execute
wave: 2
depends_on:
  - 15-02
files_modified:
  - docs/schema/arcs.md
  - docs/schema/canon.md
  - docs/schema/chapters.md
  - docs/schema/characters.md
  - docs/schema/foreshadowing.md
  - docs/schema/gate.md
  - docs/schema/knowledge.md
  - docs/schema/magic.md
  - docs/schema/names.md
  - docs/schema/plot.md
  - docs/schema/publishing.md
  - docs/schema/relationships.md
  - docs/schema/scenes.md
  - docs/schema/session.md
  - docs/schema/structure.md
  - docs/schema/timeline.md
  - docs/schema/voice.md
  - docs/schema/world.md
autonomous: true
requirements:
  - DOCS-02
user_setup: []

must_haves:
  truths:
    - "docs/schema/ directory exists with exactly 18 .md files (one per domain)"
    - "Each per-domain schema file contains the tables belonging to that domain per the section mapping in 15-RESEARCH.md"
    - "Each per-domain schema file has a '[← Documentation Index](../README.md)' back-link at the top"
    - "schema_migrations appears in structure.md (or gate.md) with its Read-only justification"
    - "architecture_gate appears in gate.md with its Read-only justification"
    - "research_notes and documentation_tasks appear in publishing.md (Utility tables assigned to publishing domain)"
    - "chapter_plot_threads appears in plot.md (not chapters.md) — junction belongs to plot domain"
    - "chapter_character_arcs appears in arcs.md (not chapters.md) — junction belongs to arcs domain"
  artifacts:
    - path: "docs/schema/structure.md"
      provides: "Structure domain schema — eras, books, acts, story_structure, arc_seven_point_beats, schema_migrations"
      contains: "Read-only justification for schema_migrations"
    - path: "docs/schema/gate.md"
      provides: "Gate domain schema — architecture_gate, gate_checklist_items, project_metrics_snapshots, pov_balance_snapshots"
      contains: "Read-only justification for architecture_gate"
    - path: "docs/schema/publishing.md"
      provides: "Publishing domain schema — publishing_assets, submission_tracker, research_notes, documentation_tasks"
      contains: "Updated Populated-by notes for research_notes and documentation_tasks"
  key_links:
    - from: "docs/schema/{domain}.md"
      to: "docs/README.md"
      via: "[← Documentation Index](../README.md) back-link"
      pattern: "\\[← Documentation Index\\]\\(\\.\\./README\\.md\\)"
---

<objective>
Split the updated docs/schema.md (from Plan 02) into 18 per-domain files under docs/schema/, applying the schema-section-to-domain mapping from 15-RESEARCH.md.

Purpose: The 2741-line monolith is split into navigable per-domain references that exactly parallel the docs/tools/ structure, giving every domain a paired tools file and schema file with the same filename.
Output: docs/schema/ directory with 18 .md files, each containing the column definitions, constraints, and Mermaid ER diagrams (where applicable) for that domain's tables.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-documentation-restructure/15-RESEARCH.md
@.planning/phases/15-documentation-restructure/15-02-SUMMARY.md

<!-- Updated monolith to split -->
@docs/schema.md
</context>

<interfaces>
<!-- Schema section → domain file mapping (from 15-RESEARCH.md — authoritative) -->

schema.md Section → docs/schema/{domain}.md:
  Section 1 (Foundation: eras, books, schema_migrations) → structure.md
    NOTE: schema_migrations is a system table; include it in structure.md with Read-only note
  Section 2 (Structure: acts, story_structure, arc_seven_point_beats) → structure.md
  Section 3 (World: cultures, factions, locations, artifacts, magic_system_elements,
              supernatural_elements, faction_political_states, object_states) → world.md
  Section 4 (Characters: characters, character_knowledge, character_beliefs,
              character_locations, injury_states, title_states) → characters.md
  Section 5 (Chapters & Scenes) → SPLIT:
    chapters.md: chapters, chapter_structural_obligations, pacing_beats, tension_measurements
    scenes.md: scenes, scene_character_goals
    chapter_plot_threads → plot.md (junction — plot threads are the entity, per research recommendation)
  Section 6 (Relationships: character_relationships, relationship_change_events,
              perception_profiles) → relationships.md
  Section 7 (Timeline & Events: events, event_participants, event_artifacts,
              travel_segments, pov_chronological_position) → timeline.md
  Section 8 (Plot & Arcs) → SPLIT:
    plot.md: plot_threads, chapter_plot_threads (junction)
    arcs.md: character_arcs, chapter_character_arcs (junction), arc_health_log,
             chekovs_gun_registry, subplot_touchpoint_log
  Section 9 (Gate & Metrics: architecture_gate, gate_checklist_items,
              project_metrics_snapshots, pov_balance_snapshots) → gate.md
    NOTE: architecture_gate has Read-only justification from Plan 02
  Section 10 (Session: session_logs, agent_run_log, open_questions,
               decisions_log) → session.md
  Section 11 (Canon & Continuity: canon_facts, continuity_issues) → canon.md
  Section 12 (Knowledge & Reader: reader_information_states, reader_reveals,
               dramatic_irony_inventory, reader_experience_notes) → knowledge.md
  Section 13 (Foreshadowing & Literary: foreshadowing_registry, prophecy_registry,
               motif_registry, motif_occurrences, thematic_mirrors,
               opposition_pairs) → foreshadowing.md
  Section 14 (Voice & Names) → SPLIT:
    voice.md: voice_profiles, voice_drift_log, supernatural_voice_guidelines
    names.md: name_registry
  Section 15 (Publishing: publishing_assets, submission_tracker) → publishing.md
  Section 16 (Utility: research_notes, documentation_tasks) → publishing.md
    (Utility merges into publishing.md — these tables are owned by publishing.py tools)
  Section on magic_system_elements → covered by world.md (not magic.md):
    Note: magic.py tools manage magic_system_elements, practitioner_abilities,
    supernatural_elements tables. magic.md schema should cover:
      - magic_system_elements, practitioner_abilities (magic domain tables)
    supernatural_elements go in magic.md too (magic.py owns those tools)
    Verify against schema.md sections which tables belong to the magic section vs world section:
    magic.md covers: magic_system_elements, practitioner_abilities, supernatural_elements
    world.md covers: cultures, factions, locations, artifacts, faction_political_states,
                     object_states (and eras/books/acts from structure section)

<!-- Per-domain file format (from 15-RESEARCH.md Pattern 2) -->
Each docs/schema/{domain}.md must follow:

```markdown
[← Documentation Index](../README.md)

# {Domain} Schema

{Optional one-paragraph description of what these tables model}

{Mermaid ER diagram if the original schema.md has one for this section;
 omit if cross-domain FKs make it misleading at domain scope;
 include only tables that belong to this domain}

> **Cross-domain FKs:** {list FKs to tables in other domains, or "None."}

## `table_name`

{One-sentence description of table purpose}

| Field | Type | Description |
|-------|------|-------------|

**Constraints:** {UNIQUE, CHECK constraints if any; omit section if none}

**Populated by:** {MCP tools that write this table}
  OR
**Read-only:** {justification — only for schema_migrations and architecture_gate}

---
```

<!-- Mermaid diagram guidance -->
- Include domain-scoped Mermaid ER diagrams if the original schema.md section has one
- Prune the diagram to show ONLY tables that belong to this domain
- For cross-domain FK lines in the original diagram, replace with a "Cross-domain FKs" prose note
- Omit Mermaid entirely for very small domains (1-2 tables) where a diagram adds no value
- Do NOT copy the top-level System Integration flowchart into any per-domain file
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/schema/ directory and split foundation, world, characters, relationships, gate, session domains</name>
  <files>
    docs/schema/structure.md
    docs/schema/world.md
    docs/schema/characters.md
    docs/schema/relationships.md
    docs/schema/gate.md
    docs/schema/session.md
  </files>
  <action>
    Create `docs/schema/` directory.

    For each of the 6 domains:

    **structure.md** — tables from Foundation + Structure sections:
    - Tables: eras, books, schema_migrations, acts, story_structure, arc_seven_point_beats
    - schema_migrations: include Read-only justification (from Plan 02 update)
    - Include Mermaid ER diagram if original schema.md has one for Foundation/Structure; prune to domain tables only
    - eras: "Populated by: `upsert_era`, `delete_era` (world.py)" (corrected by Plan 02)
    - books: "Populated by: `upsert_book`, `delete_book` (world.py)" (corrected by Plan 02)
    - acts: "Populated by: `upsert_act`, `delete_act` (world.py)" (corrected by Plan 02)

    **world.md** — tables from World section (Section 3 only; eras/books/acts moved to structure.md):
    - Tables: cultures, factions, locations, artifacts, faction_political_states, object_states
    - NOTE: magic_system_elements, supernatural_elements go in magic.md (tool ownership)
    - NOTE: practitioner_abilities goes in magic.md
    - Include Mermaid if original has one for World section

    **characters.md** — tables from Section 4:
    - Tables: characters, character_knowledge, character_beliefs, character_locations, injury_states, title_states

    **relationships.md** — tables from Section 6:
    - Tables: character_relationships, relationship_change_events, perception_profiles

    **gate.md** — tables from Section 9:
    - Tables: architecture_gate, gate_checklist_items, project_metrics_snapshots, pov_balance_snapshots
    - architecture_gate: include Read-only justification (from Plan 02 update)
    - Include Mermaid if original has one

    **session.md** — tables from Section 10:
    - Tables: session_logs, agent_run_log, open_questions, decisions_log

    Format each file following the Pattern 2 template in interfaces.
    Start each file with `[← Documentation Index](../README.md)`.
    Copy table column definitions, types, and constraints verbatim from schema.md.
    Preserve any Mermaid ER diagrams (pruned to domain tables).
  </action>
  <verify>
    ```bash
    ls docs/schema/structure.md docs/schema/world.md docs/schema/characters.md docs/schema/relationships.md docs/schema/gate.md docs/schema/session.md
    ```

    Verify back-links:
    ```bash
    grep -l "← Documentation Index" docs/schema/structure.md docs/schema/world.md docs/schema/characters.md docs/schema/relationships.md docs/schema/gate.md docs/schema/session.md
    ```
    Expected: 6 files listed.

    Verify Read-only notes present:
    ```bash
    grep -n "Read-only" docs/schema/structure.md docs/schema/gate.md
    ```
    Expected: at least 2 matches (schema_migrations in structure.md, architecture_gate in gate.md).
  </verify>
  <done>6 per-domain schema files exist with correct table content, back-links, and Read-only justifications for the 2 system tables.</done>
</task>

<task type="auto">
  <name>Task 2: Split remaining 12 domains — timeline, plot+arcs, chapters+scenes, canon, knowledge, foreshadowing, voice+names, magic, publishing</name>
  <files>
    docs/schema/timeline.md
    docs/schema/plot.md
    docs/schema/arcs.md
    docs/schema/chapters.md
    docs/schema/scenes.md
    docs/schema/canon.md
    docs/schema/knowledge.md
    docs/schema/foreshadowing.md
    docs/schema/voice.md
    docs/schema/names.md
    docs/schema/magic.md
    docs/schema/publishing.md
  </files>
  <action>
    For each domain:

    **timeline.md** — Section 7:
    - Tables: events, event_participants, event_artifacts, travel_segments, pov_chronological_position

    **plot.md** — Section 8 (Plot portion):
    - Tables: plot_threads, chapter_plot_threads (junction — placed here per research recommendation)
    - Note in chapter_plot_threads: "Junction table — MCP tools in plot.py: `link_chapter_to_plot_thread`, `unlink_chapter_from_plot_thread`, `get_plot_threads_for_chapter`"

    **arcs.md** — Section 8 (Arcs portion):
    - Tables: character_arcs, chapter_character_arcs (junction), arc_health_log, chekovs_gun_registry, subplot_touchpoint_log
    - Note in chapter_character_arcs: "Junction table — relates characters arcs to chapters they appear in"

    **chapters.md** — Section 5 (Chapters portion):
    - Tables: chapters, chapter_structural_obligations, pacing_beats, tension_measurements
    - NOTE: chapter_plot_threads goes in plot.md (not here); chapter_character_arcs goes in arcs.md (not here)
    - Add cross-domain FK note: "chapter_plot_threads and chapter_character_arcs are junction tables documented in plot.md and arcs.md respectively"

    **scenes.md** — Section 5 (Scenes portion):
    - Tables: scenes, scene_character_goals

    **canon.md** — Section 11:
    - Tables: canon_facts, continuity_issues

    **knowledge.md** — Section 12:
    - Tables: reader_information_states, reader_reveals, dramatic_irony_inventory, reader_experience_notes

    **foreshadowing.md** — Section 13:
    - Tables: foreshadowing_registry, prophecy_registry, motif_registry, motif_occurrences, thematic_mirrors, opposition_pairs

    **voice.md** — Section 14 (Voice portion):
    - Tables: voice_profiles, voice_drift_log, supernatural_voice_guidelines

    **names.md** — Section 14 (Names portion):
    - Tables: name_registry

    **magic.md** — from Section 3 (magic subsystem tables):
    - Tables: magic_system_elements, practitioner_abilities, supernatural_elements
    - These tables appear in the World or magic section of schema.md; extract them here
    - All Populated-by tools are in magic.py

    **publishing.md** — Section 15 + Section 16 (Utility merged in):
    - Tables: publishing_assets, submission_tracker, research_notes, documentation_tasks
    - research_notes: "Populated by: `upsert_research_note`, `get_research_notes`, `delete_research_note` (publishing.py)" (corrected by Plan 02)
    - documentation_tasks: "Populated by: `upsert_documentation_task`, `get_documentation_tasks`, `delete_documentation_task` (publishing.py)" (corrected by Plan 02)
    - Note: utility tables merged here because publishing.py owns all their MCP tools

    Format all files following Pattern 2 template. Include back-links. Include Mermaid ER diagrams (pruned to domain tables) where the original schema.md section has them. Add "Cross-domain FKs" prose notes for tables with FK references to tables in other domains.
  </action>
  <verify>
    Check all 12 files exist:
    ```bash
    ls docs/schema/timeline.md docs/schema/plot.md docs/schema/arcs.md docs/schema/chapters.md docs/schema/scenes.md docs/schema/canon.md docs/schema/knowledge.md docs/schema/foreshadowing.md docs/schema/voice.md docs/schema/names.md docs/schema/magic.md docs/schema/publishing.md
    ```

    Verify back-links on all 18 schema files:
    ```bash
    grep -rL "← Documentation Index" docs/schema/
    ```
    Expected: no output (all 18 files have the link).

    Verify publishing.md covers all 4 tables:
    ```bash
    grep "research_notes\|documentation_tasks\|publishing_assets\|submission_tracker" docs/schema/publishing.md | wc -l
    ```
    Expected: 4+ matches.

    Verify chapter_plot_threads is in plot.md (not chapters.md):
    ```bash
    grep -l "chapter_plot_threads" docs/schema/plot.md
    grep -c "chapter_plot_threads" docs/schema/chapters.md
    ```
    Expected: plot.md listed; chapters.md count = 0 or contains only a cross-domain reference note.
  </verify>
  <done>All 18 per-domain schema files exist in docs/schema/. Every file has back-links. Table assignments match the mapping in 15-RESEARCH.md. Publishing.md includes Utility tables. Read-only justifications are preserved from Plan 02 updates.</done>
</task>

</tasks>

<verification>
Final state check:
```bash
# Count files in docs/schema/
ls docs/schema/*.md | wc -l
# Expected: 18

# All files have back-links
grep -rL "← Documentation Index" docs/schema/
# Expected: no output

# Read-only notes present
grep -rn "Read-only" docs/schema/
# Expected: 2 matches (schema_migrations in structure.md, architecture_gate in gate.md)

# No stale "Not writable via MCP" notes
grep -rn "Not writable via MCP" docs/schema/
# Expected: 0 matches

# Utility tables in publishing.md
grep -n "research_notes\|documentation_tasks" docs/schema/publishing.md
# Expected: entries for both tables with Phase 14 tool references
```
</verification>

<success_criteria>
- docs/schema/ has exactly 18 .md files
- All 18 files have `[← Documentation Index](../README.md)` back-link
- schema_migrations is in structure.md with Read-only justification
- architecture_gate is in gate.md with Read-only justification
- research_notes and documentation_tasks are in publishing.md with Phase 14 "Populated by" notes
- chapter_plot_threads is in plot.md; chapter_character_arcs is in arcs.md
- No stale "Not writable via MCP" or "CLI seed data only" notes in any file
- Mermaid ER diagrams included where applicable, pruned to domain tables only
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-restructure/15-04-SUMMARY.md`
</output>
