---
phase: 15-documentation-restructure
plan: 05
type: execute
wave: 3
depends_on:
  - 15-03
  - 15-04
files_modified:
  - docs/README.md
  - docs/mcp-tools.md
  - docs/schema.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "docs/README.md is the master navigation index linking to all 18 tool files and all 18 schema files (36 links total)"
    - "docs/mcp-tools.md no longer exists (deleted)"
    - "docs/schema.md no longer exists (deleted)"
    - "docs/README.md contains a condensed architecture preamble (not the original 170-line full overview)"
    - "docs/README.md contains quick stats: 231 tools, 71 tables, 22 migrations"
    - "No tracked file has a broken link to docs/mcp-tools.md or docs/schema.md"
  artifacts:
    - path: "docs/README.md"
      provides: "Master documentation index"
      contains: "18 tool domain links + 18 schema domain links + quick stats"
    - path: "docs/mcp-tools.md"
      provides: "DELETED — replaced by docs/tools/{domain}.md"
    - path: "docs/schema.md"
      provides: "DELETED — replaced by docs/schema/{domain}.md"
  key_links:
    - from: "docs/README.md"
      to: "docs/tools/{domain}.md"
      via: "relative links tools/{domain}.md"
      pattern: "tools/\\w+\\.md"
    - from: "docs/README.md"
      to: "docs/schema/{domain}.md"
      via: "relative links schema/{domain}.md"
      pattern: "schema/\\w+\\.md"
---

<objective>
Overhaul docs/README.md into the master documentation index, then delete the two monoliths (docs/mcp-tools.md and docs/schema.md) to complete the restructure.

Purpose: The restructure is only complete when the monoliths are gone — otherwise per-domain files and monoliths coexist and create confusion about which is authoritative. The new README.md serves as the single entry point to all 36 per-domain documentation files.
Output: docs/README.md as master index with 36 links. docs/mcp-tools.md and docs/schema.md deleted. No broken internal doc links.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-documentation-restructure/15-RESEARCH.md
@.planning/phases/15-documentation-restructure/15-03-SUMMARY.md
@.planning/phases/15-documentation-restructure/15-04-SUMMARY.md

<!-- Current README to overhaul -->
@docs/README.md
</context>

<interfaces>
<!-- Master index structure (from 15-RESEARCH.md Pattern 3) -->
docs/README.md target structure:

```markdown
# Drafter Documentation

{Preamble: 3-4 sentences on what Drafter is, the 3-layer architecture
(SQLite DB + MCP server + CLI), and a note that full architecture detail
is in project-overview/}

## Tools Reference

| Domain | Tools | Gate | File |
|--------|-------|------|------|
| [Characters](tools/characters.md) | 19 | Free | characters |
| ...all 18 domains...

## Schema Reference

| Domain | Tables | Key Tables | File |
|--------|--------|------------|------|
| [Characters](schema/characters.md) | 6 | characters, character_knowledge... | characters |
| ...all 18 domains...

## Quick Stats

- **Total MCP tools:** 231 (153 Free, 78 Gated)
- **Total schema tables:** 71 (69 with MCP write coverage, 2 intentionally read-only)
- **Migrations:** 22
- **Tool domains:** 18

## Error Contract

[Brief summary of the 4 response types — keep the existing error contract section
from docs/mcp-tools.md but condensed to a 4-row table. Full detail is in each
per-domain tools file.]
```

<!-- Tools Reference table — 18 rows (domain | tools | gate | link) -->
Row data for Tools Reference table:
  Characters | 19 | Free | tools/characters.md
  Relationships | 9 | Free | tools/relationships.md
  Chapters | 8 | Free | tools/chapters.md
  Scenes | 12 | Free | tools/scenes.md
  World | 33 | Free | tools/world.md
  Magic | 14 | Free | tools/magic.md
  Plot | 7 | Free | tools/plot.md
  Arcs | 15 | Free | tools/arcs.md
  Gate | 6 | Free | tools/gate.md
  Names | 6 | Free | tools/names.md
  Structure | 6 | Free | tools/structure.md
  Session | 16 | Gated | tools/session.md
  Timeline | 18 | Mixed | tools/timeline.md
  Canon | 10 | Gated | tools/canon.md
  Knowledge | 12 | Gated | tools/knowledge.md
  Foreshadowing | 18 | Gated | tools/foreshadowing.md
  Voice | 9 | Gated | tools/voice.md
  Publishing | 13 | Mixed | tools/publishing.md

<!-- Schema Reference table — 18 rows (domain | tables | key tables | link) -->
Row data (domain | approximate table count | main tables):
  Characters | 6 | characters, character_knowledge, character_beliefs, character_locations, injury_states, title_states
  Relationships | 3 | character_relationships, relationship_change_events, perception_profiles
  Chapters | 4 | chapters, chapter_structural_obligations, pacing_beats, tension_measurements
  Scenes | 2 | scenes, scene_character_goals
  World | 6 | cultures, factions, locations, artifacts, faction_political_states, object_states
  Magic | 3 | magic_system_elements, practitioner_abilities, supernatural_elements
  Plot | 2 | plot_threads, chapter_plot_threads
  Arcs | 5 | character_arcs, chapter_character_arcs, arc_health_log, chekovs_gun_registry, subplot_touchpoint_log
  Gate | 4 | architecture_gate*, gate_checklist_items, project_metrics_snapshots, pov_balance_snapshots
  Names | 1 | name_registry
  Structure | 6 | eras, books, acts, story_structure, arc_seven_point_beats, schema_migrations*
  Session | 4 | session_logs, agent_run_log, open_questions, decisions_log
  Timeline | 5 | events, event_participants, event_artifacts, travel_segments, pov_chronological_position
  Canon | 2 | canon_facts, continuity_issues
  Knowledge | 4 | reader_information_states, reader_reveals, dramatic_irony_inventory, reader_experience_notes
  Foreshadowing | 6 | foreshadowing_registry, prophecy_registry, motif_registry, motif_occurrences, thematic_mirrors, opposition_pairs
  Voice | 3 | voice_profiles, voice_drift_log, supernatural_voice_guidelines
  Publishing | 4 | publishing_assets, submission_tracker, research_notes, documentation_tasks

(* = intentionally read-only system tables; add footnote or asterisk in Schema Reference table)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul docs/README.md into the master documentation index</name>
  <files>docs/README.md</files>
  <action>
    Rewrite docs/README.md as the master documentation index. The current 170-line file is the architecture overview; condense that content to a brief preamble and replace the body with navigation tables.

    Structure:

    1. **Title and preamble** (4-6 lines max):
       ```markdown
       # Drafter Documentation

       Drafter is a Python MCP server + SQLite narrative database + CLI (`novel` command) for AI-assisted novel writing. It exposes 231 MCP tools across 18 domains, backed by 71 schema tables across 22 migrations.

       Architecture: SQLite database (persistent narrative state) ← Python MCP server (FastMCP, async tools) ← Claude Code / any MCP client.
       ```
       Keep it tight — users navigating to this file want links, not an architecture lecture. The full architecture detail lives in `project-overview/`.

    2. **Tools Reference section** — table with all 18 domain rows:
       ```markdown
       ## Tools Reference

       | Domain | Tools | Gate | Reference |
       |--------|-------|------|-----------|
       | [Characters](tools/characters.md) | 19 | Free | characters |
       ```
       Use the row data in the interfaces section above. Link domain name to the tools file. Gate column: "Free", "Gated", or "Mixed".

    3. **Schema Reference section** — table with all 18 domain rows:
       ```markdown
       ## Schema Reference

       | Domain | Tables | Key Tables |
       |--------|--------|------------|
       | [Characters](schema/characters.md) | 6 | characters, character_knowledge, ... |
       ```
       Use the row data in the interfaces section above. Link domain name to the schema file. Keep "Key Tables" column brief (3-4 table names max per row to keep the table readable).
       Add a footnote: `* Intentionally read-only system table — see the schema file for justification.`

    4. **Quick Stats section**:
       ```markdown
       ## Quick Stats

       - **Total MCP tools:** 231 (153 Free, 78 Gated)
       - **Total schema tables:** 71 (69 with MCP write coverage, 2 intentionally read-only)
       - **Migrations:** 22
       - **Tool domains:** 18
       ```

    5. **Error Contract section** (condensed from mcp-tools.md):
       ```markdown
       ## Error Contract

       All MCP tools return structured data — no tool raises an uncaught exception.

       | Response Type | When | Key Fields |
       |---------------|------|-----------|
       | Normal return | Success | Tool-specific model |
       | `NotFoundResponse` | Record not found | `not_found_message: str` |
       | `ValidationFailure` | Invalid input / DB constraint | `is_valid: bool (False)`, `errors: list[str]` |
       | `GateViolation` | Gate not certified (gated tools only) | `requires_action: str` |
       ```

    The final README.md should be approximately 80-100 lines — long enough to be complete, short enough to navigate easily.

    Do NOT include: the 170-line architecture diagram, the old "Installation" section (belongs in project-overview), the old "Quick Reference" table (replaced by the new Tools/Schema Reference tables), raw SQL examples.
  </action>
  <verify>
    Check that docs/README.md has links to all 18 tool files:
    ```bash
    for domain in arcs canon chapters characters foreshadowing gate knowledge magic names plot publishing relationships scenes session structure timeline voice world; do
      if ! grep -q "tools/${domain}.md" docs/README.md; then
        echo "MISSING tools link: ${domain}"
      fi
    done
    echo "Tool link check done"
    ```
    Expected: only "Tool link check done" — no MISSING lines.

    Check that docs/README.md has links to all 18 schema files:
    ```bash
    for domain in arcs canon chapters characters foreshadowing gate knowledge magic names plot publishing relationships scenes session structure timeline voice world; do
      if ! grep -q "schema/${domain}.md" docs/README.md; then
        echo "MISSING schema link: ${domain}"
      fi
    done
    echo "Schema link check done"
    ```
    Expected: only "Schema link check done" — no MISSING lines.

    Check quick stats line:
    ```bash
    grep "231\|153.*78\|78.*153" docs/README.md
    ```
    Expected: at least one match showing correct tool totals.
  </verify>
  <done>docs/README.md contains 36 navigation links (18 tool files + 18 schema files), quick stats with 231 tools / 71 tables / 22 migrations, condensed preamble, and error contract table. File is 80-100 lines total.</done>
</task>

<task type="auto">
  <name>Task 2: Delete docs/mcp-tools.md and docs/schema.md, verify no broken references</name>
  <files>docs/mcp-tools.md, docs/schema.md</files>
  <action>
    Before deleting, verify the 36 per-domain files exist and are non-empty:
    ```bash
    ls docs/tools/*.md | wc -l   # should be 18
    ls docs/schema/*.md | wc -l  # should be 18
    ```

    If both return 18, delete the monoliths:
    ```bash
    git rm docs/mcp-tools.md
    git rm docs/schema.md
    ```
    (Use `git rm` so the deletion is staged; if not in a git context, use `rm` and stage manually.)

    After deletion, scan all tracked files for any remaining references to the deleted paths:
    ```bash
    grep -rn "docs/mcp-tools.md\|mcp-tools\.md" . \
      --include="*.md" --include="*.py" --include="*.toml" --include="*.txt" \
      --exclude-dir=".git" --exclude-dir="__pycache__" 2>/dev/null
    ```
    ```bash
    grep -rn "docs/schema\.md\|\"schema\.md\"" . \
      --include="*.md" --include="*.py" --include="*.toml" --include="*.txt" \
      --exclude-dir=".git" --exclude-dir="__pycache__" 2>/dev/null
    ```

    For any remaining reference found (other than git history):
    - If it's in a planning file (.planning/), no action needed — planning files reference historical state
    - If it's in docs/README.md, update the link to point to the appropriate per-domain file or remove it
    - If it's in project-overview/ or any other user-facing file, update the link

    After cleaning up broken references, verify docs/ has the correct final structure:
    ```bash
    ls docs/
    ```
    Expected: README.md, tools/ (directory), schema/ (directory)
    NOT expected: mcp-tools.md, schema.md
  </action>
  <verify>
    Confirm monoliths are gone:
    ```bash
    ls docs/mcp-tools.md 2>/dev/null && echo "EXISTS — should be deleted" || echo "DELETED — correct"
    ls docs/schema.md 2>/dev/null && echo "EXISTS — should be deleted" || echo "DELETED — correct"
    ```
    Expected: both lines say "DELETED — correct".

    Confirm per-domain files are in place:
    ```bash
    ls docs/tools/*.md | wc -l && ls docs/schema/*.md | wc -l
    ```
    Expected: 18 on both lines.

    Confirm no broken links to the deleted paths in user-facing docs:
    ```bash
    grep -rn "mcp-tools\.md\|/docs/schema\.md" . \
      --include="*.md" \
      --exclude-dir=".git" \
      --exclude-dir=".planning" \
      2>/dev/null | grep -v "SUMMARY\|PLAN\|CONTEXT\|RESEARCH"
    ```
    Expected: no output (no user-facing references to deleted files).
  </verify>
  <done>docs/mcp-tools.md and docs/schema.md are deleted. docs/ contains README.md + tools/ (18 files) + schema/ (18 files) — the complete restructured documentation. No broken links to deleted paths in user-facing files.</done>
</task>

</tasks>

<verification>
Complete Phase 15 acceptance check:

```bash
# 1. Both monoliths deleted
echo "=== Monoliths ==="
ls docs/mcp-tools.md 2>/dev/null || echo "mcp-tools.md: deleted (correct)"
ls docs/schema.md 2>/dev/null || echo "schema.md: deleted (correct)"

# 2. Per-domain files counts
echo "=== Per-domain file counts ==="
echo "tools files: $(ls docs/tools/*.md 2>/dev/null | wc -l | tr -d ' ') (expected 18)"
echo "schema files: $(ls docs/schema/*.md 2>/dev/null | wc -l | tr -d ' ') (expected 18)"

# 3. Master index has 36 links
echo "=== Master index links ==="
echo "tool links: $(grep -c 'tools/.*\.md' docs/README.md) (expected 18)"
echo "schema links: $(grep -c 'schema/.*\.md' docs/README.md) (expected 18)"

# 4. Quick stats present
echo "=== Quick stats ==="
grep "231\|22 migrations" docs/README.md

# 5. All per-domain files have back-links
echo "=== Back-links ==="
missing_tools=$(grep -rL "← Documentation Index" docs/tools/ 2>/dev/null | wc -l)
missing_schema=$(grep -rL "← Documentation Index" docs/schema/ 2>/dev/null | wc -l)
echo "tools files missing back-link: ${missing_tools} (expected 0)"
echo "schema files missing back-link: ${missing_schema} (expected 0)"
```
</verification>

<success_criteria>
- docs/README.md is the master index with 18 tools links + 18 schema links + quick stats (231 tools / 71 tables / 22 migrations)
- docs/mcp-tools.md deleted
- docs/schema.md deleted
- docs/ structure: README.md + tools/ (18 files) + schema/ (18 files)
- No broken links to deleted paths in user-facing documentation files
- All per-domain files (36 total) have `[← Documentation Index](../README.md)` back-links
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-restructure/15-05-SUMMARY.md`
</output>
