---
phase: 15-documentation-restructure
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - docs/tools/arcs.md
  - docs/tools/canon.md
  - docs/tools/chapters.md
  - docs/tools/characters.md
  - docs/tools/foreshadowing.md
  - docs/tools/gate.md
  - docs/tools/knowledge.md
  - docs/tools/magic.md
  - docs/tools/names.md
  - docs/tools/plot.md
  - docs/tools/publishing.md
  - docs/tools/relationships.md
  - docs/tools/scenes.md
  - docs/tools/session.md
  - docs/tools/structure.md
  - docs/tools/timeline.md
  - docs/tools/voice.md
  - docs/tools/world.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []

must_haves:
  truths:
    - "docs/tools/ directory exists with exactly 18 .md files (one per domain)"
    - "Each per-domain tools file contains only the tools belonging to that domain"
    - "Each per-domain tools file has a '[← Documentation Index](../README.md)' back-link at the top"
    - "Each per-domain tools file has a correct Index table and tool count in the header"
    - "Each per-domain tools file includes all Phase 14 tool entries (not just the pre-Phase-14 subset)"
  artifacts:
    - path: "docs/tools/characters.md"
      provides: "Characters domain tool reference"
      contains: "19 tools"
    - path: "docs/tools/world.md"
      provides: "World domain tool reference"
      contains: "33 tools"
    - path: "docs/tools/timeline.md"
      provides: "Timeline domain tool reference"
      contains: "18 tools"
  key_links:
    - from: "docs/tools/{domain}.md"
      to: "docs/README.md"
      via: "[← Documentation Index](../README.md) back-link"
      pattern: "\\[← Documentation Index\\]\\(\\.\\./README\\.md\\)"
---

<objective>
Split the updated docs/mcp-tools.md (from Plan 01, now 231 tools) into 18 per-domain files under docs/tools/.

Purpose: The monolith is unwieldy for domain-specific reference. Per-domain files let Claude Code and human readers navigate directly to the domain they need without scrolling a 3000+ line file.
Output: docs/tools/ directory with 18 .md files, each containing the complete tool reference for one domain, formatted consistently with the existing monolith style.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-documentation-restructure/15-RESEARCH.md
@.planning/phases/15-documentation-restructure/15-01-SUMMARY.md

<!-- Updated monolith to split -->
@docs/mcp-tools.md
</context>

<interfaces>
<!-- Per-domain file format (from 15-RESEARCH.md Pattern 1) -->
Each docs/tools/{domain}.md must follow this structure:

```markdown
[← Documentation Index](../README.md)

# {Domain} Tools

{One-paragraph domain description — adapt from existing mcp-tools.md section header}

**Gate status:** All tools in this domain are gate-free. / All tools in this domain are gated. / Mixed — see individual tool entries.

**{N} tools**

## Index

| Tool Name | Gate | Description |
|-----------|------|-------------|
| `tool_name` | Free | One-line description |

---

## `tool_name`

**Purpose:** {description}

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|

**Returns:** {type} — {description}

**Invocation reason:** {when an agent would call this}

**Gate status:** Gate-free. / Gated — returns `GateViolation` if gate not certified.

**Tables touched:** Reads `x`. Writes `y`.

---
```

<!-- Domain tool counts (source of truth from 15-RESEARCH.md) -->
Domain totals (required for per-file headers):
  arcs: 15, canon: 10, chapters: 8, characters: 19, foreshadowing: 18,
  gate: 6, knowledge: 12, magic: 14, names: 6, plot: 7, publishing: 13,
  relationships: 9, scenes: 12, session: 16, structure: 6, timeline: 18,
  voice: 9, world: 33

<!-- Gate status by domain (for gate status header line) -->
  Gate-free (all tools): arcs, chapters, characters, gate, magic, names, plot, relationships, scenes, structure, world
  Gated (all tools): canon, foreshadowing, knowledge, session, voice
  Mixed: publishing (8 free / 5 gated), timeline (10 free / 8 gated)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/tools/ directory and split 9 smaller domains</name>
  <files>
    docs/tools/arcs.md
    docs/tools/canon.md
    docs/tools/chapters.md
    docs/tools/gate.md
    docs/tools/names.md
    docs/tools/plot.md
    docs/tools/relationships.md
    docs/tools/scenes.md
    docs/tools/structure.md
  </files>
  <action>
    Create the `docs/tools/` directory if it does not exist.

    For each of these 9 domains — arcs (15), canon (10), chapters (8), gate (6), names (6), plot (7), relationships (9), scenes (12), structure (6) — create the per-domain file:

    1. Start with the back-link: `[← Documentation Index](../README.md)`
    2. Write the domain header: `# {Domain} Tools`
    3. Add one-paragraph domain description — adapt from the corresponding section header in docs/mcp-tools.md. If the existing section has no description prose, write a brief one-line description of what the domain manages.
    4. Add gate status line using the correct domain gate status (see interfaces above)
    5. Add tool count: `**{N} tools**`
    6. Add `## Index` section with a table listing all tools in this domain:
       `| Tool Name | Gate | Description |` — extract from docs/mcp-tools.md global Index table, filtering to this domain
    7. Add `---` divider
    8. Add each tool's full entry block, extracted verbatim from docs/mcp-tools.md domain section
       - Preserve the exact format: Purpose, Parameters table, Returns, Invocation reason, Gate status, Tables touched, `---` divider
       - Do NOT copy tools from other domains into this file
       - Include ALL tools for this domain — both pre-Phase-14 and Phase 14 additions

    Anti-patterns to avoid:
    - Do NOT include a domain-scoped Index at the top AND a repeated tool list — just the Index table + full entries
    - Do NOT change the relative path of the back-link: it must be `../README.md` (not `docs/README.md`)
    - Do NOT skip any tools — verify count matches the domain total in the interfaces section
  </action>
  <verify>
    Check all 9 files exist:
    ```bash
    ls docs/tools/arcs.md docs/tools/canon.md docs/tools/chapters.md docs/tools/gate.md docs/tools/names.md docs/tools/plot.md docs/tools/relationships.md docs/tools/scenes.md docs/tools/structure.md
    ```

    Verify each file has the back-link:
    ```bash
    grep -l "← Documentation Index" docs/tools/arcs.md docs/tools/canon.md docs/tools/chapters.md docs/tools/gate.md docs/tools/names.md docs/tools/plot.md docs/tools/relationships.md docs/tools/scenes.md docs/tools/structure.md
    ```
    Expected: 9 files listed.

    Spot-check tool counts match expected totals:
    ```bash
    for domain in arcs canon chapters gate names plot relationships scenes structure; do
      count=$(grep -c "^## \`" docs/tools/${domain}.md 2>/dev/null || echo 0)
      echo "${domain}: ${count}"
    done
    ```
    Expected: arcs:15, canon:10, chapters:8, gate:6, names:6, plot:7, relationships:9, scenes:12, structure:6
  </verify>
  <done>9 per-domain tools files exist in docs/tools/ with correct tool counts, back-links, and complete tool entries from the updated monolith.</done>
</task>

<task type="auto">
  <name>Task 2: Split 9 larger/mixed domains into per-domain files</name>
  <files>
    docs/tools/characters.md
    docs/tools/foreshadowing.md
    docs/tools/knowledge.md
    docs/tools/magic.md
    docs/tools/publishing.md
    docs/tools/session.md
    docs/tools/timeline.md
    docs/tools/voice.md
    docs/tools/world.md
  </files>
  <action>
    Apply the same extraction process as Task 1 for these 9 domains:
    characters (19), foreshadowing (18), knowledge (12), magic (14), publishing (13), session (16), timeline (18), voice (9), world (33).

    Additional notes for specific domains:

    **timeline** (mixed gate status):
    - Gate status line: "Mixed — see individual tool entries."
    - Free tools: `log_travel_segment`, `delete_event`, `delete_pov_position`, `delete_travel_segment`, all event junction tools (`add_event_participant`, `remove_event_participant`, `get_event_participants`, `add_event_artifact`, `remove_event_artifact`, `get_event_artifacts`)
    - Gated tools: the 8 pre-Phase-14 timeline CRUD tools (those that call check_gate)
    - Verify gate status for each tool from the source file if unsure

    **publishing** (mixed gate status):
    - Gate status line: "Mixed — see individual tool entries."
    - Free tools: the 8 new Phase 14 publishing tools (delete_publishing_asset, delete_submission, all documentation_task and research_note tools)
    - Gated tools: the 5 existing publishing tools (publishing_assets CRUD that were already gated)
    - Verify from source if unsure

    **world** (33 tools — largest domain):
    - All gate-free
    - 27 of these are Phase 14 additions — verify all 27 are included
    - World domain owns: cultures, factions, locations, artifacts, magic_system_elements (NOTE: magic_system_elements is distinct from the "magic" domain — do not confuse), supernatural_elements, faction_political_states, object_states, AND eras, books, acts (Phase 14 additions)
    - Wait — re-check: eras, books, acts tools are in world.py per STATE.md decisions ("[Phase 14-mcp-api-completeness]: Era model... confirmed from migration 002"). The world.py module owns upsert_era/delete_era, upsert_book/delete_book, upsert_act/delete_act. Include these in docs/tools/world.md.

    **characters** (19 tools):
    - All gate-free
    - Includes Phase 14 log tools: log_character_belief, log_character_location, log_injury_state, log_title_state, get_current_character_location, and delete tools

    Follow the same format template as Task 1. Verify counts match expected totals.
  </action>
  <verify>
    Check all 9 files exist:
    ```bash
    ls docs/tools/characters.md docs/tools/foreshadowing.md docs/tools/knowledge.md docs/tools/magic.md docs/tools/publishing.md docs/tools/session.md docs/tools/timeline.md docs/tools/voice.md docs/tools/world.md
    ```

    Verify tool counts:
    ```bash
    for domain in characters foreshadowing knowledge magic publishing session timeline voice world; do
      count=$(grep -c "^## \`" docs/tools/${domain}.md 2>/dev/null || echo 0)
      echo "${domain}: ${count}"
    done
    ```
    Expected: characters:19, foreshadowing:18, knowledge:12, magic:14, publishing:13, session:16, timeline:18, voice:9, world:33

    Verify total across all 18 files equals 231:
    ```bash
    total=$(grep -rh "^## \`" docs/tools/ | wc -l | tr -d ' ')
    echo "Total tools across all domain files: ${total}"
    ```
    Expected: 231

    Verify all 18 files have back-links:
    ```bash
    grep -rL "← Documentation Index" docs/tools/
    ```
    Expected: no output (all files have the link)
  </verify>
  <done>All 18 per-domain tools files exist in docs/tools/. Total tool count across all files is 231. Every file has the back-link to ../README.md. Mixed-gate domains (timeline, publishing) have correct per-tool gate status notes.</done>
</task>

</tasks>

<verification>
Final state check:
```bash
# Count files in docs/tools/
ls docs/tools/*.md | wc -l
# Expected: 18

# Total tool entries across all files
grep -rh "^## \`" docs/tools/ | wc -l
# Expected: 231

# All files have back-links
grep -rL "← Documentation Index" docs/tools/
# Expected: no output

# No file is empty
wc -l docs/tools/*.md | sort -n | head -5
# Expected: no file under 50 lines
```
</verification>

<success_criteria>
- docs/tools/ has exactly 18 .md files
- Total tool entry count across all 18 files = 231
- Every file has `[← Documentation Index](../README.md)` back-link
- Every file has correct tool count in header
- Every file has domain Index table
- No tool appears in the wrong domain file
- Mixed-gate domains (timeline, publishing) note gate status correctly
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-restructure/15-03-SUMMARY.md`
</output>
