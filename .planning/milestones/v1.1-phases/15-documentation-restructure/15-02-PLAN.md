---
phase: 15-documentation-restructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schema.md
autonomous: true
requirements:
  - DOCS-02
user_setup: []

must_haves:
  truths:
    - "docs/schema.md has 'Read-only: [justification]' notes for schema_migrations and architecture_gate tables"
    - "All stale 'Not writable via MCP' or 'CLI seed data only' notes are corrected for tables that Phase 14 gave write coverage"
    - "File header updated to reflect Phase 14 completion (no longer 'accurate as of Phase 11')"
  artifacts:
    - path: "docs/schema.md"
      provides: "Updated monolith ready for splitting in Plan 04"
      contains: "Read-only justifications and corrected Populated-by notes"
  key_links:
    - from: "docs/schema.md"
      to: "src/novel/tools/world.py"
      via: "Populated by notes for eras, books, acts (now have MCP write tools)"
      pattern: "upsert_era|upsert_book|upsert_act"
    - from: "docs/schema.md"
      to: "src/novel/tools/publishing.py"
      via: "Populated by notes for research_notes, documentation_tasks"
      pattern: "upsert_research_note|upsert_documentation_task"
---

<objective>
Update docs/schema.md with read-only justifications for intentionally read-only tables and correct all stale "Populated by" notes for tables that Phase 14 gave MCP write coverage.

Purpose: schema.md must be accurate before splitting in Plan 04. Stale "Not writable via MCP" notes propagated into per-domain files would permanently mislead users of the API reference.
Output: docs/schema.md with accurate "Populated by" and "Read-only" notes throughout. No structural changes — content correction only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-documentation-restructure/15-RESEARCH.md

<!-- Current monolith to update -->
@docs/schema.md
</context>

<interfaces>
<!-- Read-only tables (from 15-RESEARCH.md, HIGH confidence) -->
Read-only table justifications to add:

  schema_migrations:
    Justification: "Managed exclusively by the migration runner (`novel db migrate`).
    Writing outside the runner corrupts migration state and could cause destructive
    re-runs or skipped migrations. No MCP read or write tool exposed."

  architecture_gate:
    Justification: "Managed exclusively through the `certify_gate` tool flow. Exposing a
    direct write tool would bypass the gate enforcement mechanism. Note:
    `gate_checklist_items` (child table) has write coverage via `delete_gate_checklist_item`."

<!-- Stale "Populated by" notes to correct (from 15-RESEARCH.md) -->
Tables whose "Populated by" notes are stale post-Phase-14:
  - eras: now has upsert_era, delete_era (in world.py)
  - books: now has upsert_book, delete_book (in world.py)
  - acts: now has upsert_act, delete_act (in world.py)
  - research_notes: now has upsert_research_note, get_research_notes, delete_research_note (in publishing.py)
  - documentation_tasks: now has upsert_documentation_task, get_documentation_tasks, delete_documentation_task (in publishing.py)

Also check for any table still described as "Not writable via MCP" beyond these five — if found, verify in source and correct.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add read-only justifications to schema_migrations and architecture_gate</name>
  <files>docs/schema.md</files>
  <action>
    In docs/schema.md, find the table entry for `schema_migrations` (in Section 1 Foundation or similar system section) and add the following under its existing column description:

    ```markdown
    **Read-only:** Managed exclusively by the migration runner (`novel db migrate`). Writing outside the runner corrupts migration state and could cause destructive re-runs or skipped migrations. No MCP read or write tool is exposed for this table.
    ```

    Find the table entry for `architecture_gate` (in Section 9 Gate & Metrics) and add:

    ```markdown
    **Read-only:** Managed exclusively through the `certify_gate` tool flow. Exposing a direct write tool would bypass the gate enforcement mechanism. Note: the child table `gate_checklist_items` does have write coverage via `delete_gate_checklist_item`.
    ```

    Place these **Read-only** notes at the bottom of each table's entry block, after the column table and any constraint notes, before the `---` section divider.

    Also update the file header:
    - Change "implementation-accurate as of the build completing Phase 11" → "implementation-accurate as of Phase 14"
    - Change "16 documentation domains" → "18 documentation domains" (Phase 15 restructure will create 18 files, but the current monolith has 16 sections — update to reflect that the mapping produces 18 domains)

    Update the quick stats line to note the 2 read-only tables:
    - Add: "2 intentionally read-only tables (schema_migrations, architecture_gate)"
  </action>
  <verify>
    ```bash
    grep -n "Read-only" docs/schema.md
    ```
    Expected: at least 2 lines — one for schema_migrations and one for architecture_gate.

    ```bash
    grep -n "schema_migrations\|architecture_gate" docs/schema.md | head -20
    ```
    Both entries should appear with "Read-only:" text nearby.
  </verify>
  <done>Both schema_migrations and architecture_gate have "Read-only:" justification notes in their schema.md entries. File header updated to Phase 14 accuracy.</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale "Populated by" notes for tables now covered by Phase 14 write tools</name>
  <files>docs/schema.md</files>
  <action>
    Search schema.md for stale "Populated by" or "Writes" notes for these 5 tables and update each:

    1. **eras** table (in Section 1 Foundation):
       - Old: likely "Populated by: CLI seed data or direct DB insert" or similar
       - New: "Populated by: `upsert_era` (world.py), `delete_era` (world.py)"

    2. **books** table (in Section 1 Foundation or Section 2 Structure):
       - Old: likely "Populated by: CLI seed data or direct DB insert" or similar
       - New: "Populated by: `upsert_book` (world.py), `delete_book` (world.py)"

    3. **acts** table (in Section 2 Structure):
       - Old: likely "Populated by: CLI seed data or direct DB insert" or similar
       - New: "Populated by: `upsert_act` (world.py), `delete_act` (world.py)"

    4. **research_notes** table (in Section 16 Utility):
       - Old: "Not writable via MCP" or "CLI seed data only"
       - New: "Populated by: `upsert_research_note` (publishing.py), `get_research_notes` (publishing.py), `delete_research_note` (publishing.py)"

    5. **documentation_tasks** table (in Section 16 Utility):
       - Old: "Not writable via MCP" or "CLI seed data only"
       - New: "Populated by: `upsert_documentation_task` (publishing.py), `get_documentation_tasks` (publishing.py), `delete_documentation_task` (publishing.py)"

    Also do a full-text search for any remaining occurrences of:
    - "Not writable via MCP"
    - "CLI seed data only"
    - "CLI seed data or direct DB insert"

    For any occurrence found beyond schema_migrations and architecture_gate (which are legitimately read-only), check the corresponding table in source to determine if Phase 14 added write tools. Correct if stale.

    Additionally, scan for any "8 tools" references in the document (there shouldn't be tool counts in schema.md, but verify) and remove if found.
  </action>
  <verify>
    ```bash
    grep -n "Not writable via MCP\|CLI seed data only\|CLI seed data or direct DB insert" docs/schema.md
    ```
    Expected: 0 matches (all stale notes corrected).

    Verify the 5 target tables now reference Phase 14 tools:
    ```bash
    grep -n "upsert_era\|upsert_book\|upsert_act\|upsert_research_note\|upsert_documentation_task" docs/schema.md
    ```
    Expected: 5+ matches (one per table, possibly more for delete_ variants).
  </verify>
  <done>No stale "Not writable via MCP" or "CLI seed data only" notes remain in schema.md (except for the 2 legitimately read-only system tables). All 5 previously-stale tables now reference their Phase 14 MCP write tools.</done>
</task>

</tasks>

<verification>
Final state check:
```bash
# Confirm read-only notes present
grep -c "Read-only:" docs/schema.md
# Expected: 2

# Confirm no stale notes remain
grep -c "Not writable via MCP\|CLI seed data only" docs/schema.md
# Expected: 0

# Confirm Phase 14 tools referenced
grep -c "upsert_era\|upsert_book\|upsert_act\|upsert_research_note\|upsert_documentation_task" docs/schema.md
# Expected: 5 or more
```
</verification>

<success_criteria>
- schema.md has "Read-only:" notes for schema_migrations and architecture_gate
- All 5 stale "Populated by" notes corrected to reference Phase 14 MCP tools
- No remaining "Not writable via MCP" or "CLI seed data only" notes (except for the 2 legitimate system tables)
- File header updated to Phase 14 accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-restructure/15-02-SUMMARY.md`
</output>
