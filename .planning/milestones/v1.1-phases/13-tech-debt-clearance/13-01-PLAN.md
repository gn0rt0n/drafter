---
phase: 13-tech-debt-clearance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/novel/gate/cli.py
  - src/novel/db/cli.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-08

must_haves:
  truths:
    - "`novel gate check` output and docstring reference 36-item audit, not 34"
    - "`novel gate certify` docstring references 36 checklist items, not 34"
    - "`novel db seed --help` shows `gate_ready` (underscore) not `gate-ready` (hyphen)"
    - ".planning/REQUIREMENTS.md contains no stale '33' or '34' gate count references"
  artifacts:
    - path: "src/novel/gate/cli.py"
      provides: "Gate CLI commands with corrected count strings"
      contains: "36-item"
    - path: "src/novel/db/cli.py"
      provides: "DB seed CLI with corrected help text"
      contains: "gate_ready"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements doc free of stale gate count strings"
  key_links:
    - from: "src/novel/gate/cli.py"
      to: "module docstring and check/certify docstrings"
      via: "string literal replacement"
      pattern: "36-item|36 checklist"
    - from: "src/novel/db/cli.py"
      to: "seed command help text"
      via: "string literal replacement"
      pattern: "gate_ready"
---

<objective>
Fix all stale gate count strings in the gate CLI, the seed help text typo in the db CLI, and any stale count references in REQUIREMENTS.md.

Purpose: Three docstrings in gate/cli.py still reference "34" (the pre-7-point-structure count). The db/cli.py seed help text says "gate-ready" (hyphen) but the actual seed profile dict key is "gate_ready" (underscore). REQUIREMENTS.md is also in scope per the locked DEBT-01 decision — any stale "33"/"34" count references there must be cleared.
Output: src/novel/gate/cli.py with correct 36-item references; src/novel/db/cli.py with correct gate_ready help text; .planning/REQUIREMENTS.md verified clean.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Confirmed bug locations extracted from source before planning. -->

From src/novel/gate/cli.py:
- Line 7: `novel gate check    — run full 34-item audit...` — module docstring
- Line 9: `novel gate certify  — certify gate when all items pass (CLSG-05)` — module docstring
  NOTE: Line 9 was inspected and contains NO stale "34" string. The CONTEXT.md bug table
  listed it as a fourth occurrence, but it is "certify gate when all items pass" — no count.
  Only lines 7, 20, and 110 need replacement.
- Line 20: `"""Run full 34-item gate audit and display gap report."""` — check() docstring
- Line 110: `"""Certify the architecture gate if all 34 checklist items pass."""` — certify() docstring

From src/novel/db/cli.py:
- Line 52: `profile: str = typer.Argument("minimal", help="Seed profile name (e.g. minimal, gate-ready)")`
- Line 57: `'gate-ready'` in docstring: `"'minimal' and 'gate-ready' profiles."`

Note: The runtime behavior of gate/cli.py is already correct — `check()` uses
`total = len(GATE_QUERIES)` (dynamic, currently 36) and certify() counts from the DB.
Only the human-visible docstring/help strings are stale. No logic changes.

From .planning/REQUIREMENTS.md (pre-inspected):
The file currently shows DEBT-01 as "Gate count displays '36 items' in all output" — the
requirement descriptions already use the correct count. A grep confirms no "33" or "34"
count references are present. Task 3 performs a verification grep to confirm this remains
true and makes no edits if the file is already clean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale 34-item strings in gate/cli.py</name>
  <files>src/novel/gate/cli.py</files>
  <action>
Make exactly three string replacements in src/novel/gate/cli.py — no other changes:

1. Module docstring (line 7):
   `novel gate check    — run full 34-item audit and display gap report (CLSG-03)`
   → `novel gate check    — run full 36-item audit and display gap report (CLSG-03)`

2. check() docstring (line 20):
   `"""Run full 34-item gate audit and display gap report."""`
   → `"""Run full 36-item gate audit and display gap report."""`

3. certify() docstring (line 110):
   `"""Certify the architecture gate if all 34 checklist items pass."""`
   → `"""Certify the architecture gate if all 36 checklist items pass."""`

Line 9 of the module docstring (`novel gate certify  — certify gate when all items pass (CLSG-05)`)
was inspected and contains no "34" — no change needed there.

Do NOT change any runtime logic. Do NOT touch the `total = len(GATE_QUERIES)` line — it is already correct and dynamic.
  </action>
  <verify>
    <automated>grep -n "34" /Users/gary/writing/drafter/src/novel/gate/cli.py && echo "FAIL: stale 34 still present" || echo "PASS: no stale 34 found"</automated>
  </verify>
  <done>No occurrence of "34" in gate/cli.py; three occurrences of "36" present in the module docstring (line 7), check() docstring (line 20), and certify() docstring (line 110).</done>
</task>

<task type="auto">
  <name>Task 2: Fix gate-ready hyphen typo in db/cli.py</name>
  <files>src/novel/db/cli.py</files>
  <action>
Make exactly two string replacements in src/novel/db/cli.py — no other changes:

1. Argument help text (line 52):
   `help="Seed profile name (e.g. minimal, gate-ready)"`
   → `help="Seed profile name (e.g. minimal, gate_ready)"`

2. Function docstring (line 57):
   `"'minimal' and 'gate-ready' profiles."`
   → `"'minimal' and 'gate_ready' profiles."`

The actual seed profile dict key in src/novel/db/seed.py is `gate_ready` (underscore).
The fix makes the help text match the real key so users can copy from --help and have it work.
  </action>
  <verify>
    <automated>grep -n "gate-ready" /Users/gary/writing/drafter/src/novel/db/cli.py && echo "FAIL: hyphen form still present" || echo "PASS: no hyphen form found"</automated>
  </verify>
  <done>No occurrence of "gate-ready" in db/cli.py; both help text and docstring now show "gate_ready" (underscore).</done>
</task>

<task type="auto">
  <name>Task 3: Verify REQUIREMENTS.md has no stale gate count strings</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
The locked DEBT-01 decision places .planning/REQUIREMENTS.md in scope for stale "33"/"34"
count reference removal.

Read .planning/REQUIREMENTS.md and search for any occurrence of the strings "33" or "34"
used as gate item counts (e.g., "33-item", "34-item", "33 checks", "34 checks", "33 gate",
"34 gate"). The file was pre-inspected during planning: DEBT-01's description already reads
"Gate count displays '36 items' in all output" — no stale count strings were found.

If on execution the grep finds stale occurrences, replace them with the correct "36" count.
If no stale occurrences are found, the task is complete with no file changes.

Verification command to run:
  grep -n "\b3[34]\b" .planning/REQUIREMENTS.md

Expected result: no matches (or only matches unrelated to gate item counts, such as
"Phase 13" references — those are not stale gate counts and must not be changed).
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
text = open('/Users/gary/writing/drafter/.planning/REQUIREMENTS.md').read()
# Match '33' or '34' only when used as gate item counts (followed by space+item/check/gate)
stale = re.findall(r'\b3[34]\b.{0,20}(?:item|check|gate)', text, re.IGNORECASE)
if stale:
    print('FAIL: stale gate count references found:', stale); sys.exit(1)
else:
    print('PASS: no stale gate count references in REQUIREMENTS.md')
"
    </automated>
  </verify>
  <done>REQUIREMENTS.md contains no stale "33" or "34" gate count references. File is either unchanged (already clean) or updated to use "36".</done>
</task>

</tasks>

<verification>
Run all checks together:

```bash
grep -n "34" src/novel/gate/cli.py && echo "FAIL gate count" || echo "PASS gate count"
grep -n "gate-ready" src/novel/db/cli.py && echo "FAIL seed help" || echo "PASS seed help"
python -c "from novel.gate.cli import app; print('import OK')"
python -c "from novel.db.cli import app; print('import OK')"
python3 -c "
import re, sys
text = open('.planning/REQUIREMENTS.md').read()
stale = re.findall(r'\b3[34]\b.{0,20}(?:item|check|gate)', text, re.IGNORECASE)
if stale:
    print('FAIL REQUIREMENTS.md stale:', stale); sys.exit(1)
else:
    print('PASS REQUIREMENTS.md clean')
"
```

All five commands must pass (no stale strings, both modules import cleanly, REQUIREMENTS.md verified).
</verification>

<success_criteria>
- `src/novel/gate/cli.py` contains zero occurrences of "34"; contains "36-item" in module docstring and check() docstring; contains "36 checklist items" in certify() docstring
- `src/novel/db/cli.py` contains zero occurrences of "gate-ready"; contains "gate_ready" in both help text and docstring
- Both modules import without errors
- `.planning/REQUIREMENTS.md` contains no stale "33"/"34" gate count references
</success_criteria>

<output>
After completion, create `.planning/phases/13-tech-debt-clearance/13-01-SUMMARY.md`
</output>
