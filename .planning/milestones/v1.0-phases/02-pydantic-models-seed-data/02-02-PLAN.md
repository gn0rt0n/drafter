---
phase: 02-pydantic-models-seed-data
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/novel/models/chapters.py
  - src/novel/models/scenes.py
  - src/novel/models/plot.py
  - src/novel/models/arcs.py
  - src/novel/models/voice.py
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Chapter domain models (Chapter, ChapterStructuralObligation) exist with all SQL columns represented"
    - "Scene domain models (Scene, SceneCharacterGoal) exist with all SQL columns represented"
    - "Plot domain models (PlotThread, ChapterPlotThread, ChapterCharacterArc) exist with all SQL columns represented"
    - "Arc domain models (CharacterArc, ArcHealthLog, ChekhovGun, SubplotTouchpoint) exist with all SQL columns represented"
    - "Voice domain models (VoiceProfile, VoiceDriftLog, SupernaturalVoiceGuideline) exist with all SQL columns represented"
    - "Scene.narrative_functions JSON TEXT field has @field_validator + to_db_dict()"
  artifacts:
    - path: "src/novel/models/chapters.py"
      provides: "Chapter, ChapterStructuralObligation"
      exports: ["Chapter", "ChapterStructuralObligation"]
    - path: "src/novel/models/scenes.py"
      provides: "Scene, SceneCharacterGoal"
      exports: ["Scene", "SceneCharacterGoal"]
    - path: "src/novel/models/plot.py"
      provides: "PlotThread, ChapterPlotThread, ChapterCharacterArc"
      exports: ["PlotThread", "ChapterPlotThread", "ChapterCharacterArc"]
    - path: "src/novel/models/arcs.py"
      provides: "CharacterArc, ArcHealthLog, ChekhovGun, SubplotTouchpoint"
      exports: ["CharacterArc", "ArcHealthLog", "ChekhovGun", "SubplotTouchpoint"]
    - path: "src/novel/models/voice.py"
      provides: "VoiceProfile, VoiceDriftLog, SupernaturalVoiceGuideline"
      exports: ["VoiceProfile", "VoiceDriftLog", "SupernaturalVoiceGuideline"]
  key_links:
    - from: "src/novel/models/scenes.py"
      to: "src/novel/migrations/009_scenes.sql"
      via: "narrative_functions is JSON TEXT — field_validator + to_db_dict required"
    - from: "src/novel/models/arcs.py"
      to: "src/novel/migrations/017_arcs_chekhov.sql"
      via: "CharacterArc lives in arcs.py (not characters.py) — matches CONTEXT.md file layout"
---

<objective>
Create five domain model files covering chapters, scenes, plot threads, arcs, and voice profiles.
These represent the narrative structure and prose craft layers of the MCP server.

Purpose: Phase 4 and 5 MCP tools import Chapter, Scene, PlotThread, CharacterArc, VoiceProfile directly.
Having these in a Wave 1 parallel plan means they are ready alongside the entity models without blocking on them.

Output: chapters.py (2 models), scenes.py (2 models), plot.py (3 models), arcs.py (4 models), voice.py (3 models)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pydantic-models-seed-data/02-RESEARCH.md

## Migration source files (schema ground truth)
@src/novel/migrations/008_chapters.sql
@src/novel/migrations/009_scenes.sql
@src/novel/migrations/014_voice.sql
@src/novel/migrations/016_plot_threads.sql
@src/novel/migrations/017_arcs_chekhov.sql
@src/novel/migrations/018_scene_pacing.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chapters.py and scenes.py</name>
  <files>src/novel/models/chapters.py, src/novel/models/scenes.py</files>
  <action>
    Read migration 008 and 009 to derive exact column names. Create both files.

    **chapters.py** (migration 008):
    - `Chapter`: id, book_id, act_id, chapter_number, title, pov_character_id, status, summary, writing_plan, word_count_target, word_count_actual, in_story_date, in_story_time, scene_count, notes, canon_status, source_file, created_at, updated_at
    - `ChapterStructuralObligation`: id, chapter_id, obligation_type, description, is_met, met_in_scene_id, notes, created_at, updated_at — `is_met: bool = False`

    **scenes.py** (migration 009):
    - `Scene`: id, chapter_id, scene_number, location_id, time_marker, summary, scene_type, dramatic_question, scene_goal, obstacle, turn, consequence, emotional_function, narrative_functions, word_count_target, status, notes, canon_status, created_at, updated_at
      — `narrative_functions: list[str] | None = None` is JSON TEXT, needs @field_validator + to_db_dict()
    - `SceneCharacterGoal`: id, scene_id, character_id, goal, motivation, obstacle, outcome, notes, created_at

    Pattern reminder (from RESEARCH.md):
    - `@field_validator("narrative_functions", mode="before") @classmethod` — parse JSON string if isinstance(v, str) else return v
    - `to_db_dict()` on Scene: `d["narrative_functions"] = json.dumps(d["narrative_functions"]) if d["narrative_functions"] is not None else None`
    - All ids: `id: int | None = None`
    - All nullable FK and nullable columns: `field: TYPE | None = None`
    - Boolean INTEGER 0/1 fields: `field: bool = DEFAULT`
    - No Optional[X], no model.dict(), no Model.parse_obj()
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "from novel.models.chapters import Chapter, ChapterStructuralObligation; from novel.models.scenes import Scene, SceneCharacterGoal; s = Scene(chapter_id=1, scene_number=1, narrative_functions='[\"setup\", \"conflict\"]'); assert s.narrative_functions == ['setup', 'conflict']; print('OK')"</automated>
  </verify>
  <done>Chapter and ChapterStructuralObligation importable with all columns. Scene.narrative_functions JSON parsing works (string input coerced to list). SceneCharacterGoal importable. All ids default None.</done>
</task>

<task type="auto">
  <name>Task 2: Create plot.py, arcs.py, and voice.py</name>
  <files>src/novel/models/plot.py, src/novel/models/arcs.py, src/novel/models/voice.py</files>
  <action>
    Read migrations 016, 017, 018, 014 to derive exact column names. Create all three files.

    **plot.py** (migration 016 + junction tables in 016):
    - `PlotThread`: id, book_id, thread_type, title, description, parent_thread_id, status, introduced_in_chapter_id, resolved_in_chapter_id, stakes, notes, canon_status, created_at, updated_at
    - `ChapterPlotThread`: id, chapter_id, plot_thread_id, thread_state, developments, notes, created_at
    - `ChapterCharacterArc`: id, chapter_id, character_id, arc_state, arc_development, notes, created_at

    Note: ChapterCharacterArc is in migration 016 (plot-related junction table). Confirm column names from migration file.

    **arcs.py** (migration 017):
    - `CharacterArc`: id, character_id, arc_title, arc_type, starting_state, desired_state, current_state, introduced_in_chapter_id, resolved_in_chapter_id, notes, canon_status, created_at, updated_at
    - `ArcHealthLog`: id, character_arc_id, chapter_id, health_score, assessment, notes, logged_at
    - `ChekhovGun`: id, book_id, item_description, introduced_in_chapter_id, introduced_in_scene_id, resolved_in_chapter_id, resolved_in_scene_id, status, significance, notes, canon_status, created_at, updated_at
    - `SubplotTouchpoint`: id, plot_thread_id, chapter_id, touchpoint_type, notes, created_at

    Note: Check migration 017 carefully for arc_health_log FK — it references character_arcs, not characters directly.

    **voice.py** (migration 014):
    - `VoiceProfile`: id, character_id, speech_patterns, vocabulary_level, sentence_rhythm, verbal_tics, avoided_words, emotional_expression_style, formality_level, notes, canon_status, created_at, updated_at
    - `VoiceDriftLog`: id, character_id, chapter_id, drift_description, severity, corrective_note, created_at
    - `SupernaturalVoiceGuideline`: id, entity_type, guideline_text, example_phrases, forbidden_patterns, notes, created_at, updated_at

    Apply same patterns: id: int | None = None, X | None for nullables, bool for INTEGER 0/1.
    No JSON TEXT fields in these files (check migration files to confirm — none of these tables have JSON columns).
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "from novel.models.plot import PlotThread, ChapterPlotThread, ChapterCharacterArc; from novel.models.arcs import CharacterArc, ArcHealthLog, ChekhovGun, SubplotTouchpoint; from novel.models.voice import VoiceProfile, VoiceDriftLog, SupernaturalVoiceGuideline; print('OK')"</automated>
  </verify>
  <done>All 10 model classes (3 plot, 4 arcs, 3 voice) importable. No JSON validator errors. Field names match migration column names exactly.</done>
</task>

</tasks>

<verification>
All five domain files import cleanly:
`uv run python -c "from novel.models.chapters import Chapter; from novel.models.scenes import Scene; from novel.models.plot import PlotThread; from novel.models.arcs import CharacterArc; from novel.models.voice import VoiceProfile; print('all 5 files OK')`

Scene JSON round-trip:
`uv run python -c "from novel.models.scenes import Scene; s = Scene(chapter_id=1, scene_number=1, narrative_functions=['setup']); d = s.to_db_dict(); import json; assert isinstance(d['narrative_functions'], str); print('JSON round-trip OK')"`
</verification>

<success_criteria>
- 5 domain files created, all importable without errors
- Scene.narrative_functions coerces JSON string input to list[str]
- Scene.to_db_dict() re-encodes narrative_functions as JSON string
- CharacterArc is in arcs.py (not characters.py) — per CONTEXT.md decision
- All field names match migration SQL column names exactly
- No Optional[X] syntax, no model.dict(), no Model.parse_obj()
</success_criteria>

<output>
After completion, create `.planning/phases/02-pydantic-models-seed-data/02-02-SUMMARY.md`
</output>
