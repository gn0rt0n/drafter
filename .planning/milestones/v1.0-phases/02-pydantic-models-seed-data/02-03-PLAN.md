---
phase: 02-pydantic-models-seed-data
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/novel/models/sessions.py
  - src/novel/models/timeline.py
  - src/novel/models/canon.py
  - src/novel/models/gate.py
  - src/novel/models/publishing.py
  - src/novel/models/magic.py
  - src/novel/models/pacing.py
  - src/novel/db/seed.py
autonomous: true
requirements:
  - SEED-01
  - SEED-03
must_haves:
  truths:
    - "Seven remaining domain model files created with all tables covered (sessions, timeline, canon, gate, publishing, magic, pacing)"
    - "SessionLog carries_forward and chapters_touched JSON TEXT fields have @field_validator + to_db_dict()"
    - "novel db seed minimal succeeds and inserts data covering every domain table"
    - "Seed uses named fantasy content (not placeholder fixture names)"
    - "Seed inserts in correct FK dependency order with no IntegrityError"
    - "Seed uses cursor.lastrowid (not hardcoded IDs) for all FK references"
  artifacts:
    - path: "src/novel/models/sessions.py"
      provides: "SessionLog, AgentRunLog, ProjectMetricsSnapshot, PovBalanceSnapshot, OpenQuestion, DecisionsLogEntry"
      exports: ["SessionLog", "AgentRunLog", "ProjectMetricsSnapshot", "PovBalanceSnapshot", "OpenQuestion", "DecisionsLogEntry"]
    - path: "src/novel/models/timeline.py"
      provides: "Event, EventParticipant, EventArtifact, TravelSegment, PovChronologicalPosition"
      exports: ["Event", "EventParticipant", "EventArtifact", "TravelSegment", "PovChronologicalPosition"]
    - path: "src/novel/models/canon.py"
      provides: "CanonFact, ContinuityIssue, ForeshadowingEntry, ProphecyEntry, MotifEntry, MotifOccurrence, ThematicMirror, OppositionPair, ReaderInformationState, ReaderReveal, DramaticIronyEntry, ReaderExperienceNote"
    - path: "src/novel/models/gate.py"
      provides: "ArchitectureGate, GateChecklistItem"
    - path: "src/novel/models/publishing.py"
      provides: "PublishingAsset, SubmissionEntry, ResearchNote, DocumentationTask"
    - path: "src/novel/models/magic.py"
      provides: "MagicSystemElement, SupernaturalElement, MagicUseLog, PractitionerAbility, NameRegistryEntry"
    - path: "src/novel/models/pacing.py"
      provides: "PacingBeat, TensionMeasurement"
    - path: "src/novel/db/seed.py"
      provides: "load_seed_profile(conn, 'minimal')"
      contains: "_load_minimal"
  key_links:
    - from: "src/novel/db/seed.py"
      to: "src/novel/db/connection.py"
      via: "seed.py does NOT use get_connection() — it receives an open conn parameter (already wired in CLI)"
    - from: "src/novel/db/seed.py"
      to: "all domain tables"
      via: "Insertion order follows FK dependency graph from RESEARCH.md — eras first, then books, cultures, factions, characters, acts, locations, chapters, scenes, then remaining tables"
---

<objective>
Create the remaining seven domain model files and implement the minimal seed profile.
These are the session/timeline/canon/gate/publishing/magic/pacing domains plus the actual seed data.

Purpose: Completes the full 14-domain model coverage. The seed implementation fills the Phase 1 stub
so `novel db seed minimal` works, giving every Phase 3+ MCP tool real data to return.

Output: 7 model files (29 model classes) + seed.py with `_load_minimal()` inserting named fantasy data
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pydantic-models-seed-data/02-RESEARCH.md

## Migration source files (schema ground truth)
@src/novel/migrations/011_magic.sql
@src/novel/migrations/015_events_timeline.sql
@src/novel/migrations/018_scene_pacing.sql
@src/novel/migrations/019_sessions.sql
@src/novel/migrations/020_gate_metrics.sql
@src/novel/migrations/021_literary_publishing.sql

## Existing stub to replace
@src/novel/db/seed.py
@src/novel/db/connection.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 remaining domain model files</name>
  <files>src/novel/models/sessions.py, src/novel/models/timeline.py, src/novel/models/canon.py, src/novel/models/gate.py, src/novel/models/publishing.py, src/novel/models/magic.py, src/novel/models/pacing.py</files>
  <action>
    Read each migration file listed in context and derive model fields exactly from SQL column names.

    **sessions.py** (migration 019 + 020 metrics tables):
    - `SessionLog`: id, started_at, closed_at, summary, carried_forward, word_count_delta, chapters_touched, notes
      — `carried_forward: list[str] | None = None` (JSON TEXT), `chapters_touched: list[int] | None = None` (JSON TEXT)
      — Needs @field_validator for BOTH fields + to_db_dict() that re-encodes both to JSON strings
    - `AgentRunLog`: id, session_id, agent_name, tool_name, input_summary, output_summary, duration_ms, success, error_message, ran_at — `success: bool = True`
    - `ProjectMetricsSnapshot`: derive columns from 020 migration
    - `PovBalanceSnapshot`: derive columns from 020 migration
    - `OpenQuestion`: derive columns from 020 migration
    - `DecisionsLogEntry`: derive columns from 021 migration (decisions_log table)

    **timeline.py** (migration 015):
    - `Event`: id, chapter_id, location_id, event_type, title, description, in_story_date, in_story_time, duration_description, significance, notes, canon_status, created_at, updated_at
    - `EventParticipant`: id, event_id, character_id, role, notes, created_at
    - `EventArtifact`: id, event_id, artifact_id, role, notes, created_at
    - `TravelSegment`: id, character_id, from_location_id, to_location_id, start_chapter_id, end_chapter_id, travel_method, duration_days, distance_description, notes, created_at
    - `PovChronologicalPosition`: id, character_id, chapter_id, in_story_date, in_story_time, location_id, notes, created_at, updated_at

    **canon.py** (migration 021 literary tables):
    - `CanonFact`: id, domain, fact_text, source_chapter_id, source_event_id, certainty_level, supersedes_fact_id, notes, canon_status, created_at
    - `ContinuityIssue`: id, issue_description, severity, chapter_id, detected_at, resolved_at, resolution_notes, created_at
    - `ForeshadowingEntry`: id, item_description, plant_chapter_id, plant_scene_id, payoff_chapter_id, payoff_scene_id, status, foreshadow_type, notes, canon_status, created_at
    - `ProphecyEntry`: id, prophecy_text, source_character_id, introduced_in_chapter_id, fulfillment_chapter_id, fulfillment_status, notes, canon_status, created_at
    - `MotifEntry`: id, motif_name, description, first_occurrence_chapter_id, significance, notes, canon_status, created_at
    - `MotifOccurrence`: id, motif_id, chapter_id, scene_id, occurrence_description, notes, created_at
    - `ThematicMirror`: id, element_a_id, element_a_type, element_b_id, element_b_type, mirror_relationship, notes, created_at — element_a_id/element_b_id are plain int (polymorphic, no FK constraint — see RESEARCH.md Pitfall 5)
    - `OppositionPair`: id, entity_a_id, entity_a_type, entity_b_id, entity_b_type, opposition_type, notes, created_at
    - `ReaderInformationState`: id, chapter_id, information_domain, state_description, notes, created_at, updated_at
    - `ReaderReveal`: id, chapter_id, scene_id, character_id, information_revealed, planned_vs_actual, notes, created_at
    - `DramaticIronyEntry`: id, chapter_id, character_id, irony_description, reader_knows, character_doesnt_know, resolution_chapter_id, notes, created_at
    - `ReaderExperienceNote`: id, chapter_id, note_type, note_text, notes, created_at

    **gate.py** (migration 020):
    - `ArchitectureGate`: id, book_id, certified_at, certified_by, certification_notes, is_certified, created_at, updated_at — `is_certified: bool = False`
    - `GateChecklistItem`: id, gate_id, item_key, item_description, category, is_passed, evidence_query, last_checked_at, notes, created_at, updated_at — `is_passed: bool = False`

    **publishing.py** (migration 021 publishing tables):
    - `PublishingAsset`: id, asset_type, title, content, version, target_agent, target_publisher, notes, created_at, updated_at
    - `SubmissionEntry`: id, publishing_asset_id, agent_name, agency, submission_date, status, response_date, response_notes, notes, created_at, updated_at
    - `ResearchNote`: id, topic, content, source, relevance, notes, created_at
    - `DocumentationTask`: id, task_description, priority, status, due_chapter_id, completed_at, notes, created_at, updated_at

    **magic.py** (migration 011):
    - `MagicSystemElement`: id, element_name, element_type, description, rules, limitations, cost, introduced_in_chapter_id, notes, canon_status, created_at, updated_at
    - `SupernaturalElement`: id, element_name, element_type, description, rules, limitations, origin, introduced_in_chapter_id, notes, canon_status, created_at, updated_at
    - `MagicUseLog`: id, character_id, magic_element_id, chapter_id, scene_id, use_description, cost_paid, consequence, notes, created_at
    - `PractitionerAbility`: id, character_id, magic_element_id, proficiency_level, learned_in_chapter_id, notes, created_at, updated_at
    - `NameRegistryEntry`: id, name_text, name_type, character_id, culture_id, introduced_in_chapter_id, linguistic_notes, notes, created_at

    **pacing.py** (migration 018):
    - `PacingBeat`: id, chapter_id, scene_id, beat_type, description, tension_level, notes, created_at
    - `TensionMeasurement`: id, chapter_id, tension_score, measurement_basis, notes, created_at

    For each file, confirm all column names match by reading the actual SQL. Do NOT invent columns.
    Apply standard patterns: id: int | None = None, X | None for nullable, bool for INTEGER 0/1.
    SessionLog needs @field_validator for both JSON fields and to_db_dict() re-encoding both.
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run python -c "from novel.models.sessions import SessionLog, AgentRunLog, ProjectMetricsSnapshot, PovBalanceSnapshot, OpenQuestion, DecisionsLogEntry; from novel.models.timeline import Event, EventParticipant, EventArtifact, TravelSegment, PovChronologicalPosition; from novel.models.canon import CanonFact, ContinuityIssue, ForeshadowingEntry, ProphecyEntry, MotifEntry, MotifOccurrence, ThematicMirror, OppositionPair, ReaderInformationState, ReaderReveal, DramaticIronyEntry, ReaderExperienceNote; from novel.models.gate import ArchitectureGate, GateChecklistItem; from novel.models.publishing import PublishingAsset, SubmissionEntry, ResearchNote, DocumentationTask; from novel.models.magic import MagicSystemElement, SupernaturalElement, MagicUseLog, PractitionerAbility, NameRegistryEntry; from novel.models.pacing import PacingBeat, TensionMeasurement; print('OK')"</automated>
  </verify>
  <done>All 29 model classes across 7 files importable without errors. SessionLog JSON fields (carried_forward, chapters_touched) parse from string input and re-encode via to_db_dict(). Boolean fields coerce 0/1 correctly. ThematicMirror.element_a_id is plain int (no FK — documented in docstring).</done>
</task>

<task type="auto">
  <name>Task 2: Implement minimal seed profile in seed.py</name>
  <files>src/novel/db/seed.py</files>
  <action>
    Replace the Phase 1 stub in `src/novel/db/seed.py` with the real implementation.

    The file receives an open `sqlite3.Connection` from the caller (the CLI command already handles
    opening/closing via `get_connection()`). The seed loader does NOT call `get_connection()` itself.

    **Seed content requirements (locked decisions from CONTEXT.md):**
    - Named fantasy content — plausible fantasy names, NOT "Character 1" or real novel characters
    - Richer than minimum: 2 books, 5+ characters, 3+ chapters
    - Populates every domain table with at least 1 representative row
    - Inline Python dict data — NO separate JSON or SQL files

    **Fantasy world to build (invent freely):**
    - Era: "The Age of Embers" (era_id captured)
    - Books: "The Void Between Stars" (book 1), "The Shattered Meridian" (book 2) — (book_id_1, book_id_2 captured)
    - Culture: "Kaelthari" culture (culture_id captured)
    - Faction: "The Obsidian Court" faction — leader_character_id nullable so insert before characters (faction_id captured)
    - Characters (5+): protagonist, antagonist, mentor, ally, rival — each with role, culture_id, home_era_id
      (char_id_* captured for later FKs)
    - Act: Act 1 of book 1 (act_id captured)
    - Location: "The Ashen Citadel" with sensory_profile JSON (location_id captured)
    - Chapters (3+): chapters of book 1 with pov_character_id (chapter_id_* captured)
    - Scenes (2+ per chapter): in chapters (scene_id_* captured)
    - Then all remaining domain tables: 1 row each using captured IDs

    **Insertion order — STRICT FK dependency (from RESEARCH.md Pattern 6):**
    1. eras → books → cultures → factions → characters → acts → locations → chapters → scenes
    2. artifacts, magic_system_elements, supernatural_elements
    3. character_relationships, perception_profiles, voice_profiles
    4. events, pov_chronological_position, travel_segments
    5. plot_threads, character_arcs, chapter_plot_threads, chapter_structural_obligations
    6. chekovs_gun_registry, arc_health_log, scene_character_goals
    7. pacing_beats, tension_measurements
    8. session_logs, architecture_gate, gate_checklist_items
    9. open_questions, decisions_log, canon_facts
    10. foreshadowing_registry, prophecy_registry, motif_registry, motif_occurrences
    11. reader_information_states, reader_reveals, dramatic_irony_inventory, faction_political_states
    12. practitioner_abilities, name_registry, publishing_assets, submission_tracker
    13. character_knowledge, character_beliefs, character_locations, injury_states, title_states
    14. voice_drift_log, event_participants, event_artifacts, chapter_character_arcs
    15. subplot_touchpoint_log, object_states, continuity_issues, opposition_pairs
    16. thematic_mirrors, reader_experience_notes, research_notes, documentation_tasks
    17. project_metrics_snapshots, pov_balance_snapshots, agent_run_log, magic_use_log
    18. relationship_change_events

    **ID capture pattern (RESEARCH.md Pitfall 4 — never hardcode IDs):**
    ```python
    cur = conn.execute("INSERT INTO eras (name, ...) VALUES (?, ...)", (...,))
    era_id = cur.lastrowid
    ```

    **Session log JSON fields — serialize before INSERT:**
    ```python
    conn.execute("INSERT INTO session_logs (carried_forward, chapters_touched) VALUES (?, ?)",
                 (json.dumps(["Review chapter 2 pacing"]), json.dumps([chapter_id_1])))
    ```

    **Location sensory_profile — serialize to JSON:**
    ```python
    import json
    sensory = json.dumps({"sight": "obsidian walls etched with dying stars", "smell": "ash and old iron"})
    ```

    **Commit batches** — commit after each logical group (entities, then relationships, then junction tables):
    ```python
    conn.commit()
    ```

    **File structure:**
    ```python
    import sqlite3
    import json

    def load_seed_profile(conn: sqlite3.Connection, profile: str) -> None:
        profiles = {"minimal": _load_minimal}
        if profile not in profiles:
            raise ValueError(f"Unknown seed profile '{profile}'. Available: {list(profiles)}")
        profiles[profile](conn)

    def _load_minimal(conn: sqlite3.Connection) -> None:
        """Insert minimal seed data in FK dependency order."""
        # Phase 1: Foundation entities
        # Phase 2: Characters and world
        # Phase 3: Narrative structure
        # Phase 4: Domain tables
    ```

    After writing, verify manually that the function signature is compatible with the existing CLI wiring (it receives `conn: sqlite3.Connection`).
  </action>
  <verify>
    <automated>cd /Users/gary/writing/drafter && uv run novel db reset --yes 2>/dev/null; uv run novel db seed minimal && uv run novel db status</automated>
  </verify>
  <done>`novel db seed minimal` completes without error. `novel db status` shows row counts greater than 0 for key tables (books, characters, chapters, scenes, locations). No IntegrityError during insertion.</done>
</task>

</tasks>

<verification>
Full end-to-end seed verification:
```
uv run novel db reset --yes
uv run novel db seed minimal
uv run novel db status
```
Expect: status shows migration version 21, all key tables populated.

Spot-check character count:
```
uv run python -c "
import sqlite3
from novel.db.connection import get_connection
with get_connection() as conn:
    n = conn.execute('SELECT COUNT(*) FROM characters').fetchone()[0]
    print(f'characters: {n}')
    n = conn.execute('SELECT COUNT(*) FROM books').fetchone()[0]
    print(f'books: {n}')
"
```
Expect characters >= 5, books >= 2.
</verification>

<success_criteria>
- All 29 model classes across 7 files importable
- SessionLog JSON fields parse string → list and re-encode via to_db_dict()
- `novel db seed minimal` completes without errors
- At least 2 books, 5 characters, 3 chapters, 2 scenes in the seeded database
- Every domain table has at least 1 row (verified in TEST-02 in plan 02-04)
- Seed uses named fantasy content (not placeholder names)
- All IDs captured via cursor.lastrowid (no hardcoded IDs)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pydantic-models-seed-data/02-03-SUMMARY.md`
</output>
