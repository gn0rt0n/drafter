---
phase: 11-update-schema-cli-mcp-and-planning-docs-to-support-7-point-structure-and-3-act-7-point-integration
plan: "04"
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-04, STRUCT-05, STRUCT-06, STRUCT-07]

must_haves:
  truths:
    - "REQUIREMENTS.md contains a '### MCP — Structure Domain' section with STRUCT-01 through STRUCT-07 definitions"
    - "SEED-02 in REQUIREMENTS.md reads '36 architecture gate checklist items' not '33'"
    - "The traceability table at the bottom of REQUIREMENTS.md has 7 rows mapping STRUCT-01 through STRUCT-07 to Phase 11"
    - "The coverage totals at the bottom of REQUIREMENTS.md reflect 138 v1 requirements (131 + 7 new)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "STRUCT-01 through STRUCT-07 definitions + updated SEED-02 + Phase 11 traceability rows"
      contains: "STRUCT-01"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "STRUCT-01 through STRUCT-07 IDs referenced in Phase 11 now defined in REQUIREMENTS.md"
      pattern: "STRUCT-0[1-7]"
---

<objective>
Update REQUIREMENTS.md to add the 7 STRUCT-* requirement definitions that were implemented in Phase 11 but never added to the canonical requirements file.

Purpose: Phase 11 implemented STRUCT-01 through STRUCT-07 and they are referenced in ROADMAP.md and plan frontmatter, but REQUIREMENTS.md — the single source of truth for all project requirements — has no STRUCT-* entries. Additionally, SEED-02 in REQUIREMENTS.md still references "33 architecture gate checklist items" when the gate now has 36. This gap-closure plan fixes requirements traceability.

Output: An updated `.planning/REQUIREMENTS.md` with a new "### MCP — Structure Domain" section, a corrected SEED-02 count, and 7 new rows in the traceability table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-update-schema-cli-mcp-and-planning-docs-to-support-7-point-structure-and-3-act-7-point-integration/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STRUCT-01 through STRUCT-07 to REQUIREMENTS.md and fix SEED-02 count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make three targeted edits to `.planning/REQUIREMENTS.md`:

**Edit 1 — Fix SEED-02 count.**

Find the line:
```
- [x] **SEED-02**: Gate-ready seed profile satisfies all 33 architecture gate checklist items so gate certification can be tested with seed data
```

Replace with:
```
- [x] **SEED-02**: Gate-ready seed profile satisfies all 36 architecture gate checklist items so gate certification can be tested with seed data
```

**Edit 2 — Add new "### MCP — Structure Domain" section.**

Insert the following block immediately BEFORE the line `### CLI — Database Commands` (which currently appears after the `### MCP — Publishing Domain` section):

```markdown
### MCP — Structure Domain

- [x] **STRUCT-01**: Migration 022 creates `story_structure` table (one row per book, `UNIQUE(book_id)`) and `arc_seven_point_beats` table (`UNIQUE(arc_id, beat_type)`) with all 7-point beat FK columns
- [x] **STRUCT-02**: `StoryStructure` and `ArcSevenPointBeat` Pydantic models exist in `src/novel/models/structure.py` and are exported from `novel.models`
- [x] **STRUCT-03**: Gate extended with `struct_story_beats` check (category: structure) — SQL confirms all books have a `story_structure` row with all 7 beat chapter FKs populated
- [x] **STRUCT-04**: Gate extended with `arcs_seven_point_beats` check (category: plot) — SQL confirms all POV character arcs have all 7 `arc_seven_point_beats` rows defined
- [x] **STRUCT-05**: Claude can retrieve the story-level 7-point structure for a book (`get_story_structure`) and retrieve all beat records for a character arc (`get_arc_beats`)
- [x] **STRUCT-06**: Claude can create or update the story-level 7-point structure for a book, including 3-act alignment chapter FKs (`upsert_story_structure`)
- [x] **STRUCT-07**: Claude can create or update a single arc beat record with `beat_type` validation against the 7 canonical values (`upsert_arc_beat`)
```

**Edit 3 — Add Phase 11 rows to the traceability table and update coverage totals.**

In the `## Traceability` section at the bottom of the file, append the following 7 rows to the table (after the last existing row `| TEST-04 | Phase 10: CLI Completion & Integration Testing | Complete |`):

```
| STRUCT-01 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-02 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-03 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-04 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-05 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-06 | Phase 11: 7-Point Structure & Gate Extension | Complete |
| STRUCT-07 | Phase 11: 7-Point Structure & Gate Extension | Complete |
```

Then update the coverage totals block (currently ending the file) from:
```
**Coverage:**
- v1 requirements: 131 total
- Mapped to phases: 131
- Unmapped: 0
```

To:
```
**Coverage:**
- v1 requirements: 138 total
- Mapped to phases: 138
- Unmapped: 0
```

Also update the `*Last updated:` line at the very bottom of the file to today's date:
```
*Last updated: 2026-03-09 after Phase 11 gap closure (added STRUCT-01 through STRUCT-07)*
```

Do NOT change any other content in the file. Do NOT reorder sections. Do NOT alter any existing requirement text except SEED-02's "33" → "36".
  </action>
  <verify>
    <automated>grep -c "STRUCT-0" /Users/gary/writing/drafter/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - `grep "STRUCT-01" .planning/REQUIREMENTS.md` returns a line containing "Migration 022 creates"
    - `grep "STRUCT-07" .planning/REQUIREMENTS.md` returns a line containing "upsert_arc_beat"
    - `grep "SEED-02" .planning/REQUIREMENTS.md` returns a line containing "36 architecture gate checklist items"
    - `grep "v1 requirements: 138" .planning/REQUIREMENTS.md` returns the coverage total line
    - `grep -c "STRUCT-0" .planning/REQUIREMENTS.md` returns 14 (7 in section + 7 in traceability table)
  </done>
</task>

</tasks>

<verification>
After the task completes, verify REQUIREMENTS.md is consistent:

1. `grep "STRUCT-01" .planning/REQUIREMENTS.md` — must return 2 matches (section definition + traceability row)
2. `grep "36 architecture" .planning/REQUIREMENTS.md` — must return SEED-02 line
3. `grep "33 architecture" .planning/REQUIREMENTS.md` — must return NO matches (stale count removed)
4. `grep "v1 requirements: 138" .planning/REQUIREMENTS.md` — must return 1 match
5. `grep "Phase 11: 7-Point Structure" .planning/REQUIREMENTS.md` — must return 7 matches
</verification>

<success_criteria>
REQUIREMENTS.md is the canonical source of truth for all 138 v1 requirements. STRUCT-01 through STRUCT-07 are defined in a "### MCP — Structure Domain" section with the same style as all other MCP domain sections. SEED-02 correctly states "36" gate checklist items. The traceability table at the bottom maps all 7 STRUCT IDs to Phase 11. The coverage total reads 138.
</success_criteria>

<output>
After completion, create `.planning/phases/11-update-schema-cli-mcp-and-planning-docs-to-support-7-point-structure-and-3-act-7-point-integration/11-04-SUMMARY.md`
</output>
