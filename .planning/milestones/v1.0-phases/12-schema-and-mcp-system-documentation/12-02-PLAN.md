---
phase: 12-schema-and-mcp-system-documentation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schema.md
autonomous: true
requirements: []
# Note: Phase 12 is documentation-only with no formal requirement IDs (Requirements: TBD in ROADMAP).
# This plan covers the full SQLite schema reference documentation.

must_haves:
  truths:
    - "docs/schema.md documents all 71 tables across 22 migrations"
    - "Each domain section has a Mermaid ER diagram showing tables and FK relationships"
    - "Each table entry includes purpose, fields, FK prose, lifecycle note, and gate flag"
    - "Field names in docs/schema.md match actual migration SQL files (not design doc)"
    - "The 16 schema domains are organized semantically, not by migration file number"
  artifacts:
    - path: "docs/schema.md"
      provides: "Complete SQLite schema reference with domain grouping and Mermaid diagrams"
      min_lines: 800
      contains: "erDiagram"
  key_links:
    - from: "docs/schema.md"
      to: "src/novel/migrations/*.sql"
      via: "field names and types derived from SQL files"
      pattern: "Every field name must match column name in migration SQL"
    - from: "docs/schema.md table entries"
      to: "src/novel/tools/*.py"
      via: "Populated by lifecycle notes reference actual tool names"
---

<objective>
Create docs/schema.md — the implementation-accurate reference for the complete SQLite schema: 71 tables across 22 migrations, organized into 16 semantic domains, each with a Mermaid ER diagram and per-table entries covering purpose, fields, FK relationships, lifecycle notes, and gate flags.

Purpose: System admins need to understand the database structure — what tables exist, what they store, how they relate, and which MCP tools write to them. This is the authoritative reference (not the historical design doc which has confirmed field-name drift).
Output: docs/schema.md — comprehensive schema reference derived directly from migration SQL files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-schema-and-mcp-system-documentation/12-CONTEXT.md
@.planning/phases/12-schema-and-mcp-system-documentation/12-RESEARCH.md
</context>

<interfaces>
<!-- Source files the executor must read before writing any documentation content. -->
<!-- CRITICAL: Read migration SQL files for field names — DO NOT use project-research/database-schema.md for this. -->

Migration files (ground truth for field names):
  src/novel/migrations/001_schema_tracking.sql
  src/novel/migrations/002_core_books_eras.sql
  src/novel/migrations/003_acts.sql
  src/novel/migrations/004_cultures.sql
  src/novel/migrations/005_factions.sql
  src/novel/migrations/006_locations.sql
  src/novel/migrations/007_characters.sql
  src/novel/migrations/008_chapters.sql
  src/novel/migrations/009_scenes.sql
  src/novel/migrations/010_artifacts.sql
  src/novel/migrations/011_magic.sql
  src/novel/migrations/012_relationships.sql
  src/novel/migrations/013_character_state.sql
  src/novel/migrations/014_voice.sql
  src/novel/migrations/015_events_timeline.sql
  src/novel/migrations/016_plot_threads.sql
  src/novel/migrations/017_arcs_chekhov.sql
  src/novel/migrations/018_scene_pacing.sql
  src/novel/migrations/019_sessions.sql
  src/novel/migrations/020_gate_metrics.sql
  src/novel/migrations/021_literary_publishing.sql
  src/novel/migrations/022_seven_point_structure.sql

Tool files (for "Populated by" lifecycle notes):
  src/novel/tools/characters.py
  src/novel/tools/relationships.py
  src/novel/tools/chapters.py
  src/novel/tools/scenes.py
  src/novel/tools/world.py
  src/novel/tools/magic.py
  src/novel/tools/plot.py
  src/novel/tools/arcs.py
  src/novel/tools/gate.py
  src/novel/tools/session.py
  src/novel/tools/timeline.py
  src/novel/tools/canon.py
  src/novel/tools/knowledge.py
  src/novel/tools/foreshadowing.py
  src/novel/tools/voice.py
  src/novel/tools/names.py
  src/novel/tools/publishing.py
  src/novel/tools/structure.py

Domain grouping (16 domains — from RESEARCH.md):
  Foundation: schema_migrations, eras, books (001, 002)
  Structure: acts, story_structure, arc_seven_point_beats (003, 022)
  World: cultures, factions, locations, artifacts, magic_system_elements, supernatural_elements, faction_political_states, object_states (004, 005, 006, 010, 011, 021 partial)
  Characters: characters, character_knowledge, character_beliefs, character_locations, injury_states, title_states (007, 013)
  Chapters & Scenes: chapters, scenes, scene_character_goals, pacing_beats, tension_measurements, chapter_structural_obligations, chapter_plot_threads (008, 009, 016 partial, 018)
  Relationships: character_relationships, relationship_change_events, perception_profiles (012)
  Timeline & Events: events, event_participants, event_artifacts, travel_segments, pov_chronological_position (015)
  Plot & Arcs: plot_threads, character_arcs, chapter_character_arcs, arc_health_log, chekovs_gun_registry, subplot_touchpoint_log (016 partial, 017)
  Gate & Metrics: architecture_gate, gate_checklist_items, project_metrics_snapshots, pov_balance_snapshots (020)
  Session: session_logs, agent_run_log, open_questions, decisions_log (019, 021 partial)
  Canon & Continuity: canon_facts, continuity_issues (021 partial)
  Knowledge & Reader: reader_information_states, reader_reveals, dramatic_irony_inventory, reader_experience_notes (021 partial)
  Foreshadowing & Literary: foreshadowing_registry, prophecy_registry, motif_registry, motif_occurrences, thematic_mirrors, opposition_pairs (021 partial)
  Voice & Names: voice_profiles, voice_drift_log, supernatural_voice_guidelines, name_registry (014, 021 partial)
  Publishing: publishing_assets, submission_tracker (021 partial)
  Utility: research_notes, documentation_tasks (021 partial)

Key structural notes (from RESEARCH.md — verified):
  - character_relationships stores pairs in canonical order: min(a,b) as character_a_id
  - thematic_mirrors.element_a_id/b_id are plain INTEGER — polymorphic references, no SQL FK constraints
  - Migration 021 is a single file with 24 tables — the largest migration by far
  - open_questions uses column 'question' (not 'question_text') — known design-doc drift
  - session_logs.chapters_touched and carried_forward are JSON TEXT arrays
  - scenes.narrative_functions is a JSON TEXT array
  - story_structure has UNIQUE(book_id) — one row per book
  - arc_seven_point_beats has UNIQUE(arc_id, beat_type) — one beat type per arc
  - voice_profiles has UNIQUE(character_id) — one profile per character
  - character_relationships has UNIQUE(character_a_id, character_b_id) — one row per dyad

Gate-gated write domains (tools call check_gate()):
  session, timeline, canon, knowledge, foreshadowing, voice, publishing

Gate-free domains (no check_gate()):
  characters, relationships, chapters, scenes, world, magic, plot, arcs, gate, names, structure

Tables with no MCP write tools (note as "Not writable via MCP"):
  schema_migrations, event_participants, event_artifacts, pacing_beats, tension_measurements,
  title_states, research_notes, documentation_tasks, reader_experience_notes

Mermaid ER cardinality notation:
  ||--|| : exactly one to exactly one
  ||--o{ : one to zero-or-many
  }o--|| : zero-or-many to one
  o|--|| : zero-or-one to one (nullable FK)
  Cross-domain FKs: shown as prose notes inline, NOT as diagram edges
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Read all migration SQL files and produce docs/schema.md</name>
  <files>docs/schema.md</files>
  <action>
    STEP 1 — READ ALL SOURCE FILES FIRST:
    Read every migration SQL file listed in the interfaces section above (001-022). Read the key tool files to gather "Populated by" notes for each table (which tool functions write to it). Do NOT skip any migration file — field names must be derived from SQL, not memory.

    STEP 2 — WRITE docs/schema.md with the following structure:

    **File header:**
    - Title: "Drafter Database Schema Reference"
    - Status note: "Generated from migration SQL files — implementation-accurate as of the build completing Phase 11"
    - Quick stats: 71 tables, 22 migrations, 16 documentation domains
    - Note about the historical design doc: "project-research/database-schema.md is the pre-build design document; field names there may differ from actual columns"

    **System Integration section (at the top, before domain sections):**
    - Prose description of how the 16 domains depend on each other
    - Mermaid flowchart (not ER diagram) showing high-level domain dependencies:
      Foundation -> Structure, World, Characters
      World -> Characters (culture/faction FK)
      Characters -> Relationships, Chapters & Scenes
      Chapters & Scenes -> Plot & Arcs, Timeline & Events
      All domains -> Gate & Metrics (gate enforcement)
      Session captures activity across all domains
    - Cross-domain FK summary table (3 columns: from_table.field, to_table, domain_boundary)

    **For each of the 16 domains, write a domain section:**

    Domain section structure:
    ```
    ## [Domain Name]

    [1-2 sentence overview of what this domain stores and its role in the system]

    ```mermaid
    erDiagram
        [tables in this domain with fields and FK arrows between tables IN THIS DOMAIN ONLY]
    ```

    > **Cross-domain FKs:** [prose list of FK fields that reference tables in other domains]

    ### `table_name`

    [1-2 sentence purpose statement]

    | Field | Type | Description |
    |-------|------|-------------|
    | id | INTEGER PK | Primary key |
    | field_name | TEXT | [purpose sentence for non-obvious fields; "Standard audit field" for created_at/updated_at/notes/source_file] |
    | fk_field | INTEGER FK | References [table.field] — [what this relationship means] |

    **Constraints:** [UNIQUE constraints, CHECK constraints if any — derived from SQL]

    **Populated by:** [Which MCP tools write to this table, e.g., "`upsert_character` (characters domain)"; for tables with no MCP writes: "Not writable via MCP — [internal use / migration only / CLI only]"]

    **Gate flag:** [Omit entirely if gate-free; add "⚠️ Gate-enforced writes — MCP write tools require gate certification" if the domain's tools call check_gate()]
    ```

    **Mermaid ER diagram rules:**
    - One diagram per domain section
    - Include only tables that belong to this domain
    - Show FK arrows between tables WITHIN the domain using standard cardinality notation
    - DO NOT draw edges to tables in other domains (cross-domain FKs go in the prose note below the diagram)
    - Use `INTEGER PK`, `TEXT`, `INTEGER FK`, `REAL` as field type labels (abbreviated)
    - Nullable FK fields: use `o|--||` cardinality

    **Field table rules:**
    - Every field from the CREATE TABLE SQL must appear in the table
    - id: "INTEGER PK | Primary key" (standard, brief)
    - created_at, updated_at: "TEXT | Standard audit timestamp"
    - notes, source_file: "TEXT | Standard annotation field" (or omit description column if obvious)
    - FK fields: full prose — "References `characters.id` — the character this knowledge belongs to"
    - Business logic fields: full purpose sentence — what it stores, why it matters
    - JSON TEXT fields: note "JSON TEXT array/object" and what structure it stores

    **Gate flag rule:**
    - Add the ⚠️ note to any table that is written by a gated tool (check_gate() present in that tool module)
    - Gate-free tables: omit the note entirely (no "Gate-free" marker needed)

    **Tables with no MCP write tools** (list from interfaces):
    schema_migrations, event_participants, event_artifacts, pacing_beats, tension_measurements, title_states, research_notes, documentation_tasks, reader_experience_notes
    For these: "Populated by: Not writable via MCP — [appropriate reason: migration runner / internal SQLite population / no write path implemented]"

    Write all 16 domain sections in order, covering all 71 tables.
  </action>
  <verify>
    <automated>test -f /Users/gary/writing/drafter/docs/schema.md && wc -l /Users/gary/writing/drafter/docs/schema.md && grep -c "erDiagram" /Users/gary/writing/drafter/docs/schema.md</automated>
  </verify>
  <done>
    - docs/schema.md exists with 800+ lines
    - grep -c "erDiagram" returns 16 (one per domain section)
    - All 71 table names appear in the file (verify by spot-checking: schema_migrations, arc_seven_point_beats, submission_tracker, thematic_mirrors, pov_chronological_position)
    - Field names match migration SQL (spot check: open_questions has field 'question' not 'question_text')
    - Gate flag present in Session, Timeline, Canon, Knowledge, Foreshadowing, Voice, Publishing domains
  </done>
</task>

</tasks>

<verification>
- docs/schema.md exists
- wc -l output >= 800
- grep -c "erDiagram" output == 16
- grep "open_questions" shows the table and "question" field (not "question_text")
- grep "arc_seven_point_beats" shows UNIQUE(arc_id, beat_type) constraint noted
- grep "⚠️ Gate-enforced" appears in Session, Timeline, Canon, Knowledge, Foreshadowing, Voice, Publishing sections
- All 71 table names appear in the file
</verification>

<success_criteria>
- docs/schema.md is the implementation-accurate schema reference
- All 71 tables documented with field names matching actual migration SQL
- 16 Mermaid ER diagrams (one per domain), cross-domain FKs in prose
- Per-table: purpose, fields, constraints, lifecycle note, gate flag where applicable
- System Integration section at top with domain dependency map
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-and-mcp-system-documentation/12-02-SUMMARY.md`
</output>
